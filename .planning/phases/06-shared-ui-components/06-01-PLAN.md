---
phase: 06-shared-ui-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Button.tsx
  - src/components/ui/Input.tsx
  - src/components/ui/Select.tsx
  - src/components/ui/Textarea.tsx
  - src/components/ui/Toggle.tsx
autonomous: true
requirements: [PRTL-01, PRTL-02, PRTL-03, PRTL-04, PRTL-05, COMP-06, COMP-07]

must_haves:
  truths:
    - "Button rendered in portal context shows cyan-to-teal gradient with cyan focus ring"
    - "Input/Select/Textarea rendered in portal context show cyan focus ring"
    - "Toggle in portal context shows cyan-600 active state"
    - "Toggle admin checked state is indigo-600, not blue-600"
    - "Textarea admin focus ring is indigo-500, rounded-md, slate palette"
    - "All gray-* classes in these 5 files are replaced with slate-*"
  artifacts:
    - path: "src/components/ui/Button.tsx"
      provides: "Portal variant with cyan gradient"
      contains: "portal"
    - path: "src/components/ui/Input.tsx"
      provides: "Portal variant with cyan focus ring"
      contains: "portal"
    - path: "src/components/ui/Select.tsx"
      provides: "Portal variant with cyan focus ring"
      contains: "portal"
    - path: "src/components/ui/Textarea.tsx"
      provides: "Portal variant with cyan focus ring and admin indigo fix"
      contains: "portal"
    - path: "src/components/ui/Toggle.tsx"
      provides: "Portal variant with cyan active state and admin indigo fix"
      contains: "portal"
  key_links:
    - from: "src/components/ui/Button.tsx"
      to: "portal pages"
      via: "variant='portal' prop"
      pattern: "variant.*portal"
---

<objective>
Add portal variants to Button, Input, Select, Textarea, and Toggle components, plus fix admin color corrections on Toggle (blue->indigo) and Textarea (blue->indigo, rounded-md, slate palette).

Purpose: Portal pages currently render with admin indigo branding. These 5 component variants enable the full cyan brand experience across all 29 portal pages. Toggle and Textarea also have admin-side color bugs (blue instead of indigo).
Output: 5 updated component files with `variant="portal"` support and admin color corrections.
</objective>

<execution_context>
@C:/Users/18cla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18cla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-ui-components/06-CONTEXT.md
@.planning/action-plan/PRIORITIES.md
@src/components/ui/Button.tsx
@src/components/ui/Input.tsx
@src/components/ui/Select.tsx
@src/components/ui/Textarea.tsx
@src/components/ui/Toggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add portal variant to Button, Input, and Select</name>
  <files>src/components/ui/Button.tsx, src/components/ui/Input.tsx, src/components/ui/Select.tsx</files>
  <action>
**Button.tsx** (Blocking #8):
- Add `"portal"` to the variant union type: `variant?: "primary" | "secondary" | "danger" | "ghost" | "portal"`
- Add portal entry to `variantStyles` map:
  ```
  portal: "bg-gradient-to-r from-cyan-500 to-teal-600 text-white shadow-lg shadow-cyan-600/20 hover:from-cyan-600 hover:to-teal-700 focus-visible:ring-cyan-500"
  ```
- Replace any `gray-*` classes with `slate-*` throughout the file
- Ensure `focus-visible:ring` (not `focus:ring`) on all variants

**Input.tsx** (Blocking #9):
- Add `variant?: "admin" | "portal"` prop to InputProps interface (default "admin")
- Admin variant keeps current indigo focus ring: `focus-visible:ring-indigo-500`
- Portal variant: `focus-visible:ring-cyan-500`
- Apply variant-based focus ring class in the className construction
- Replace any `gray-*` with `slate-*`, `rounded-lg` with `rounded-md`
- Ensure `focus-visible:ring` (not `focus:ring`)

**Select.tsx** (Blocking #10):
- Add `variant?: "admin" | "portal"` prop to SelectProps (default "admin")
- Admin: `focus-visible:ring-indigo-500`
- Portal: `focus-visible:ring-cyan-500`
- Replace any `gray-*` with `slate-*`, `rounded-lg` with `rounded-md`
- Ensure `focus-visible:ring` (not `focus:ring`)
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && npx tsc --noEmit src/components/ui/Button.tsx src/components/ui/Input.tsx src/components/ui/Select.tsx 2>&1 | head -20 && echo "---gray check---" && grep -c "gray-" src/components/ui/Button.tsx src/components/ui/Input.tsx src/components/ui/Select.tsx</automated>
  </verify>
  <done>Button has portal variant with cyan gradient. Input and Select have portal variant with cyan focus ring. No gray-* classes remain in any of the 3 files. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add portal variant to Textarea and Toggle with admin color fixes</name>
  <files>src/components/ui/Textarea.tsx, src/components/ui/Toggle.tsx</files>
  <action>
**Textarea.tsx** (Blocking #11, High B-07):
- Add `variant?: "admin" | "portal"` prop to TextareaProps (default "admin")
- Admin variant: `focus-visible:ring-indigo-500` (fix from `focus:ring-blue-500`)
- Portal variant: `focus-visible:ring-cyan-500`
- Fix `rounded-lg` to `rounded-md`
- Replace all `gray-*` with `slate-*` throughout
- Ensure `focus-visible:ring` (not `focus:ring`)

**Toggle.tsx** (Blocking #12, High B-06):
- Add `variant?: "admin" | "portal"` prop to ToggleProps (default "admin")
- Admin checked state: fix `bg-blue-600` to `bg-indigo-600`
- Admin unchecked: fix `bg-gray-200` to `bg-slate-200`
- Admin focus: fix `focus:ring` to `focus-visible:ring-indigo-500`
- Portal checked state: `bg-cyan-600`
- Portal focus: `focus-visible:ring-cyan-500`
- Replace all remaining `gray-*` with `slate-*`
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && npx tsc --noEmit src/components/ui/Textarea.tsx src/components/ui/Toggle.tsx 2>&1 | head -20 && echo "---gray check---" && grep -c "gray-" src/components/ui/Textarea.tsx src/components/ui/Toggle.tsx && echo "---blue check---" && grep -c "blue-" src/components/ui/Textarea.tsx src/components/ui/Toggle.tsx</automated>
  </verify>
  <done>Textarea has portal variant with cyan focus ring and admin indigo fix. Toggle has portal variant with cyan-600 active and admin indigo-600 fix. No gray-* or blue-* classes remain in either file.</done>
</task>

</tasks>

<verification>
- `grep -rn "portal" src/components/ui/Button.tsx src/components/ui/Input.tsx src/components/ui/Select.tsx src/components/ui/Textarea.tsx src/components/ui/Toggle.tsx` shows portal variant in all 5 files
- `grep -rn "gray-" src/components/ui/Button.tsx src/components/ui/Input.tsx src/components/ui/Select.tsx src/components/ui/Textarea.tsx src/components/ui/Toggle.tsx` returns 0 matches
- `grep -rn "blue-" src/components/ui/Toggle.tsx src/components/ui/Textarea.tsx` returns 0 matches
- `npx tsc --noEmit` passes for all 5 files
</verification>

<success_criteria>
All 5 form/action components accept `variant="portal"` and render with cyan branding. Admin Toggle uses indigo-600, admin Textarea uses indigo focus ring and rounded-md. No gray-* or blue-* palette classes remain in any of these 5 files.
</success_criteria>

<output>
After completion, create `.planning/phases/06-shared-ui-components/06-01-SUMMARY.md`
</output>
