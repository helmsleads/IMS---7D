---
phase: 06-shared-ui-components
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Skeleton.tsx
  - src/components/ui/Table.tsx
  - src/components/ui/StatusBadge.tsx
  - src/components/ui/BarcodeScanner.tsx
  - src/components/ui/CommandPalette.tsx
  - src/components/ui/ConfirmDialog.tsx
  - src/components/ui/DropdownMenu.tsx
  - src/components/ui/EmptyState.tsx
  - src/components/ui/ErrorBoundary.tsx
  - src/components/ui/FetchError.tsx
  - src/components/ui/ProductImage.tsx
  - src/components/internal/PickScanner.tsx
  - src/components/internal/PackScanner.tsx
  - src/components/internal/ShipScanner.tsx
  - src/components/internal/ReceivingScanner.tsx
  - src/components/internal/PutawayScanner.tsx
autonomous: true
requirements: [COMP-15, COMP-16, COMP-17]

must_haves:
  truths:
    - "No shared UI component in src/components/ui/ uses gray-* palette classes"
    - "No scanner component in the 5 listed files contains dark: variant classes"
    - "StatusBadge uses semantic status-to-variant mapping, not Tailwind class string keys"
  artifacts:
    - path: "src/components/ui/Skeleton.tsx"
      provides: "Slate-palette skeleton"
      contains: "slate-"
    - path: "src/components/ui/Table.tsx"
      provides: "Slate-palette table"
      contains: "slate-"
    - path: "src/components/ui/StatusBadge.tsx"
      provides: "Semantic status mapping"
      contains: "status"
  key_links:
    - from: "src/components/ui/StatusBadge.tsx"
      to: "pages using StatusBadge"
      via: "component import"
      pattern: "status.*variant"
---

<objective>
Complete the gray-to-slate migration across all remaining shared UI components not covered by Plans 01-04, remove dark mode classes from 5 scanner components, and refactor StatusBadge from Tailwind class keys to semantic status mapping.

Purpose: The gray palette is inconsistent with the design system's slate palette. Dark mode classes are dead code (MASTER.md is light-mode only). StatusBadge's Tailwind-key-based mapping is fragile and hard to maintain.
Output: 11 updated UI component files, 5 updated scanner files, 1 refactored StatusBadge.
</objective>

<execution_context>
@C:/Users/18cla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18cla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-ui-components/06-CONTEXT.md
@.planning/action-plan/PRIORITIES.md
@src/components/ui/StatusBadge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gray-to-slate migration across remaining UI components</name>
  <files>src/components/ui/Skeleton.tsx, src/components/ui/Table.tsx, src/components/ui/BarcodeScanner.tsx, src/components/ui/CommandPalette.tsx, src/components/ui/ConfirmDialog.tsx, src/components/ui/DropdownMenu.tsx, src/components/ui/EmptyState.tsx, src/components/ui/ErrorBoundary.tsx, src/components/ui/FetchError.tsx, src/components/ui/ProductImage.tsx</files>
  <action>
Systematic find-and-replace across all 10 files (High B-38 partial):

For each file, replace ALL `gray-*` Tailwind classes with their `slate-*` equivalents:
- `gray-50` -> `slate-50`
- `gray-100` -> `slate-100`
- `gray-200` -> `slate-200`
- `gray-300` -> `slate-300`
- `gray-400` -> `slate-400`
- `gray-500` -> `slate-500`
- `gray-600` -> `slate-600`
- `gray-700` -> `slate-700`
- `gray-800` -> `slate-800`
- `gray-900` -> `slate-900`

This includes all variants (bg-, text-, border-, ring-, divide-, placeholder-, etc.).

**IMPORTANT:** Only replace Tailwind class usages. Do NOT replace:
- Variable names like `grayColor`
- String comparisons or enum values
- Comments mentioning gray

After replacement, verify each file still looks correct by checking the context of each replacement.

Files and expected gray- count (approximate):
- Skeleton.tsx: ~12 instances
- Table.tsx: ~16 instances
- BarcodeScanner.tsx: several
- CommandPalette.tsx: several
- ConfirmDialog.tsx: 1-2
- DropdownMenu.tsx: several
- EmptyState.tsx: 3
- ErrorBoundary.tsx: several
- FetchError.tsx: several
- ProductImage.tsx: 1-2
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && echo "Remaining gray- count:" && grep -c "gray-" src/components/ui/Skeleton.tsx src/components/ui/Table.tsx src/components/ui/BarcodeScanner.tsx src/components/ui/CommandPalette.tsx src/components/ui/ConfirmDialog.tsx src/components/ui/DropdownMenu.tsx src/components/ui/EmptyState.tsx src/components/ui/ErrorBoundary.tsx src/components/ui/FetchError.tsx src/components/ui/ProductImage.tsx 2>/dev/null && npx tsc --noEmit src/components/ui/Skeleton.tsx src/components/ui/Table.tsx 2>&1 | head -10</automated>
  </verify>
  <done>All 10 UI component files use slate-* instead of gray-*. Zero gray- class references remain in any of these files. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dark mode classes from scanner components</name>
  <files>src/components/internal/PickScanner.tsx, src/components/internal/PackScanner.tsx, src/components/internal/ShipScanner.tsx, src/components/internal/ReceivingScanner.tsx, src/components/internal/PutawayScanner.tsx</files>
  <action>
Remove all `dark:` variant classes from these 5 scanner components (High B-39):

Search for the pattern `dark:` followed by any Tailwind class and remove those class segments. Common patterns to remove:
- `dark:bg-*`
- `dark:text-*`
- `dark:border-*`
- `dark:hover:*`
- `dark:ring-*`

For example, a className like `"bg-white dark:bg-gray-800 text-slate-900 dark:text-white"` becomes `"bg-white text-slate-900"`.

**IMPORTANT:** Be careful with string concatenation and template literals. Remove only the dark: class segments, preserving surrounding whitespace correctly (no double spaces or leading/trailing spaces in className strings).

Also do NOT change any other classes. This task is strictly dark: removal only.
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && echo "Remaining dark: count:" && grep -c "dark:" src/components/internal/PickScanner.tsx src/components/internal/PackScanner.tsx src/components/internal/ShipScanner.tsx src/components/internal/ReceivingScanner.tsx src/components/internal/PutawayScanner.tsx 2>/dev/null && npx tsc --noEmit src/components/internal/PickScanner.tsx src/components/internal/PackScanner.tsx 2>&1 | head -10</automated>
  </verify>
  <done>All 5 scanner component files have zero dark: variant classes. No other classes were affected. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor StatusBadge to semantic status mapping</name>
  <files>src/components/ui/StatusBadge.tsx</files>
  <action>
Refactor StatusBadge variant mapping (High B-40):

**Current pattern** (fragile):
```typescript
const variantMap = {
  "bg-green-100": { ... },
  "bg-red-100": { ... },
  // Tailwind class strings as keys
};
```

**New pattern** (semantic):
Replace the Tailwind class key lookup with a direct status-to-Badge-variant mapping:
```typescript
type StatusType = 'success' | 'error' | 'warning' | 'info' | 'neutral' | 'pending';

const statusVariantMap: Record<StatusType, { variant: BadgeVariant; label?: string }> = {
  success: { variant: 'success' },
  error: { variant: 'error' },
  warning: { variant: 'warning' },
  info: { variant: 'info' },
  neutral: { variant: 'default' },
  pending: { variant: 'warning' },
};
```

- Map the existing Tailwind class keys to their semantic equivalents so callers can migrate
- If callers currently pass a Tailwind class string, add a compatibility layer that maps old keys to new semantic statuses (log a console.warn in dev to encourage migration)
- OR if StatusBadge is only used internally, update all call sites to use the new semantic API directly. Check `grep -rn "StatusBadge" src/` to see usage patterns.
- Replace any `gray-*` with `slate-*` in the file
- Ensure the dot indicator and text rendering still work correctly
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && npx tsc --noEmit src/components/ui/StatusBadge.tsx 2>&1 | head -20 && echo "---old pattern check---" && grep -c "bg-green-100\|bg-red-100" src/components/ui/StatusBadge.tsx 2>/dev/null && echo "---gray check---" && grep -c "gray-" src/components/ui/StatusBadge.tsx 2>/dev/null</automated>
  </verify>
  <done>StatusBadge uses semantic status-to-variant mapping instead of Tailwind class string keys. All callers compile correctly. No gray-* classes remain. The component is more maintainable and type-safe.</done>
</task>

</tasks>

<verification>
- `grep -rn "gray-" src/components/ui/Skeleton.tsx src/components/ui/Table.tsx src/components/ui/BarcodeScanner.tsx src/components/ui/CommandPalette.tsx src/components/ui/ConfirmDialog.tsx src/components/ui/DropdownMenu.tsx src/components/ui/EmptyState.tsx src/components/ui/ErrorBoundary.tsx src/components/ui/FetchError.tsx src/components/ui/ProductImage.tsx src/components/ui/StatusBadge.tsx` returns 0 matches
- `grep -rn "dark:" src/components/internal/PickScanner.tsx src/components/internal/PackScanner.tsx src/components/internal/ShipScanner.tsx src/components/internal/ReceivingScanner.tsx src/components/internal/PutawayScanner.tsx` returns 0 matches
- `npx tsc --noEmit` passes for all modified files
- Full build `npx next build` should not introduce new errors
</verification>

<success_criteria>
Gray-to-slate migration is complete across all remaining shared UI components. Dark mode classes are removed from all 5 scanner components. StatusBadge uses semantic status mapping instead of Tailwind class keys. All files compile without TypeScript errors.
</success_criteria>

<output>
After completion, create `.planning/phases/06-shared-ui-components/06-05-SUMMARY.md`
</output>
