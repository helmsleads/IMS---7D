---
phase: 06-shared-ui-components
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/internal/PickScanner.tsx
  - src/components/internal/PackScanner.tsx
  - src/components/internal/ShipScanner.tsx
  - src/components/internal/ReceivingScanner.tsx
  - src/components/internal/PutawayScanner.tsx
autonomous: true
requirements: [COMP-16]

must_haves:
  truths:
    - "No scanner component in the 5 listed files contains dark: variant classes"
  artifacts:
    - path: "src/components/internal/PickScanner.tsx"
      provides: "Light-mode-only scanner"
      contains: "className"
    - path: "src/components/internal/PackScanner.tsx"
      provides: "Light-mode-only scanner"
      contains: "className"
    - path: "src/components/internal/ShipScanner.tsx"
      provides: "Light-mode-only scanner"
      contains: "className"
    - path: "src/components/internal/ReceivingScanner.tsx"
      provides: "Light-mode-only scanner"
      contains: "className"
    - path: "src/components/internal/PutawayScanner.tsx"
      provides: "Light-mode-only scanner"
      contains: "className"
  key_links: []
---

<objective>
Remove all dark mode (`dark:`) variant classes from 5 scanner components that are dead code in the light-mode-only application.

Purpose: The application uses light-mode only (per MASTER.md). These `dark:` classes are dead code that adds visual noise and maintenance burden.
Output: 5 updated scanner component files with zero `dark:` class references.
</objective>

<execution_context>
@C:/Users/18cla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18cla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-ui-components/06-CONTEXT.md
@.planning/action-plan/PRIORITIES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dark mode classes from all 5 scanner components</name>
  <files>src/components/internal/PickScanner.tsx, src/components/internal/PackScanner.tsx, src/components/internal/ShipScanner.tsx, src/components/internal/ReceivingScanner.tsx, src/components/internal/PutawayScanner.tsx</files>
  <action>
Remove all `dark:` variant classes from these 5 scanner components (High B-39):

Search for the pattern `dark:` followed by any Tailwind class and remove those class segments. Common patterns to remove:
- `dark:bg-*`
- `dark:text-*`
- `dark:border-*`
- `dark:hover:*`
- `dark:ring-*`

For example, a className like `"bg-white dark:bg-gray-800 text-slate-900 dark:text-white"` becomes `"bg-white text-slate-900"`.

**IMPORTANT:** Be careful with string concatenation and template literals. Remove only the dark: class segments, preserving surrounding whitespace correctly (no double spaces or leading/trailing spaces in className strings).

Also do NOT change any other classes. This task is strictly dark: removal only.
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && echo "Remaining dark: count:" && grep -c "dark:" src/components/internal/PickScanner.tsx src/components/internal/PackScanner.tsx src/components/internal/ShipScanner.tsx src/components/internal/ReceivingScanner.tsx src/components/internal/PutawayScanner.tsx 2>/dev/null && npx tsc --noEmit src/components/internal/PickScanner.tsx src/components/internal/PackScanner.tsx 2>&1 | head -10</automated>
  </verify>
  <done>All 5 scanner component files have zero dark: variant classes. No other classes were affected. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `grep -rn "dark:" src/components/internal/PickScanner.tsx src/components/internal/PackScanner.tsx src/components/internal/ShipScanner.tsx src/components/internal/ReceivingScanner.tsx src/components/internal/PutawayScanner.tsx` returns 0 matches
- `npx tsc --noEmit` passes for all 5 files
</verification>

<success_criteria>
Dark mode classes are removed from all 5 scanner components. No other classes were modified. All files compile without TypeScript errors.
</success_criteria>

<output>
After completion, create `.planning/phases/06-shared-ui-components/06-06-SUMMARY.md`
</output>
