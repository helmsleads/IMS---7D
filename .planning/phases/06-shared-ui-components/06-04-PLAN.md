---
phase: 06-shared-ui-components
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useReducedMotion.ts
  - src/components/ui/StatCard.tsx
  - src/components/ui/charts/DonutChart.tsx
  - src/components/ui/charts/HorizontalBarChart.tsx
  - src/components/ui/charts/MiniBarChart.tsx
  - src/components/ui/charts/MiniLineChart.tsx
  - src/components/ui/charts/MiniSparkline.tsx
  - src/components/ui/charts/ScatterChart.tsx
  - src/components/ui/charts/StackedBarChart.tsx
  - src/components/ui/charts/TreemapChart.tsx
  - src/components/ui/charts/WaterfallChart.tsx
  - src/components/ui/charts/BulletChart.tsx
  - src/components/ui/charts/CalendarHeatmap.tsx
  - src/components/ui/charts/GaugeChart.tsx
autonomous: true
requirements: [COMP-04, COMP-13, COMP-14, XCUT-01]

must_haves:
  truths:
    - "All 12 chart components have aria-label prop and role=img wrapper"
    - "All 12 chart components pass isAnimationActive={false} when user prefers reduced motion"
    - "StatCard iconColor defaults to indigo-50/indigo-600 instead of blue"
    - "StatCard useAnimatedNumber hook respects prefers-reduced-motion"
    - "useReducedMotion hook exists and returns boolean based on matchMedia"
  artifacts:
    - path: "src/hooks/useReducedMotion.ts"
      provides: "Reusable reduced-motion detection hook"
      exports: ["useReducedMotion"]
    - path: "src/components/ui/StatCard.tsx"
      provides: "Indigo default iconColor and motion-aware animated number"
      contains: "indigo"
    - path: "src/components/ui/charts/DonutChart.tsx"
      provides: "Accessible chart with ARIA and reduced-motion"
      contains: "role=\"img\""
  key_links:
    - from: "src/hooks/useReducedMotion.ts"
      to: "src/components/ui/charts/*.tsx"
      via: "import { useReducedMotion }"
      pattern: "useReducedMotion"
    - from: "src/hooks/useReducedMotion.ts"
      to: "src/components/ui/StatCard.tsx"
      via: "import { useReducedMotion }"
      pattern: "useReducedMotion"
---

<objective>
Create a useReducedMotion hook, add ARIA attributes and reduced-motion guards to all 12 chart components, fix StatCard default iconColor to indigo, and make StatCard's useAnimatedNumber hook respect reduced motion.

Purpose: Charts are completely inaccessible to screen readers (no labels, no roles) and ignore the user's motion preferences. StatCard uses blue defaults instead of the design system's indigo and animates numbers even when the user prefers reduced motion.
Output: 1 new hook file, 12 updated chart files, 1 updated StatCard file.
</objective>

<execution_context>
@C:/Users/18cla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18cla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-ui-components/06-CONTEXT.md
@.planning/action-plan/PRIORITIES.md
@src/components/ui/StatCard.tsx
@src/components/ui/charts/DonutChart.tsx
@src/components/ui/charts/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useReducedMotion hook and fix StatCard</name>
  <files>src/hooks/useReducedMotion.ts, src/components/ui/StatCard.tsx</files>
  <action>
**Create `src/hooks/useReducedMotion.ts`:**
```typescript
import { useState, useEffect } from 'react';

export function useReducedMotion(): boolean {
  const [prefersReducedMotion, setPrefersReducedMotion] = useState(false);

  useEffect(() => {
    const mql = window.matchMedia('(prefers-reduced-motion: reduce)');
    setPrefersReducedMotion(mql.matches);
    const handler = (e: MediaQueryListEvent) => setPrefersReducedMotion(e.matches);
    mql.addEventListener('change', handler);
    return () => mql.removeEventListener('change', handler);
  }, []);

  return prefersReducedMotion;
}
```

**StatCard.tsx** (High B-04, High B-37):
- Fix iconColor default: change `bg-blue-50` to `bg-indigo-50` and `text-blue-600` to `text-indigo-600` in the default iconColor value/fallback
- In the `useAnimatedNumber` hook (defined within StatCard or nearby):
  - Import and use `useReducedMotion` hook
  - If `prefersReducedMotion` is true, skip the animation and return the target value immediately (no requestAnimationFrame loop)
  - If false, animate as before
- Replace any `gray-*` with `slate-*`
- Replace any remaining `blue-*` with `indigo-*`
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && npx tsc --noEmit src/hooks/useReducedMotion.ts src/components/ui/StatCard.tsx 2>&1 | head -20 && echo "---blue check---" && grep -c "blue-" src/components/ui/StatCard.tsx 2>/dev/null && echo "---hook check---" && grep -c "useReducedMotion" src/components/ui/StatCard.tsx</automated>
  </verify>
  <done>useReducedMotion hook exists and exports correctly. StatCard defaults to indigo-50/indigo-600 iconColor. useAnimatedNumber returns target value immediately when reduced motion is preferred. No blue-* classes remain in StatCard.</done>
</task>

<task type="auto">
  <name>Task 2: Add ARIA and reduced-motion to all 12 chart components</name>
  <files>src/components/ui/charts/DonutChart.tsx, src/components/ui/charts/HorizontalBarChart.tsx, src/components/ui/charts/MiniBarChart.tsx, src/components/ui/charts/MiniLineChart.tsx, src/components/ui/charts/MiniSparkline.tsx, src/components/ui/charts/ScatterChart.tsx, src/components/ui/charts/StackedBarChart.tsx, src/components/ui/charts/TreemapChart.tsx, src/components/ui/charts/WaterfallChart.tsx, src/components/ui/charts/BulletChart.tsx, src/components/ui/charts/CalendarHeatmap.tsx, src/components/ui/charts/GaugeChart.tsx</files>
  <action>
For ALL 12 chart components (High B-12, High B-13):

**ARIA attributes:**
- Add `ariaLabel?: string` prop to each chart's props interface
- Wrap the chart's return JSX in a `<div role="img" aria-label={ariaLabel}>` wrapper. If ariaLabel is not provided, use a sensible default like `"Chart"` or the chart type name (e.g., `"Donut chart"`, `"Bar chart"`).
- Inside the role="img" wrapper, add a visually-hidden `<table>` with the chart's data for screen reader access. Use a `<span className="sr-only">` or similar pattern. The table should have:
  - A `<caption>` matching the ariaLabel
  - `<thead>` with column headers from the data keys
  - `<tbody>` with rows from the data values
  - If the chart data structure is too complex for a simple table (e.g., CalendarHeatmap), provide a summary `<p className="sr-only">` with key statistics instead.

**Reduced motion:**
- Import `useReducedMotion` from `@/hooks/useReducedMotion`
- Call `const prefersReducedMotion = useReducedMotion();`
- Pass `isAnimationActive={!prefersReducedMotion}` to all Recharts components that accept it (e.g., `<PieChart>`, `<BarChart>`, `<LineChart>`, `<Area>`, `<Bar>`, `<Line>`, `<Pie>`, etc.)
- For non-Recharts charts (CalendarHeatmap, GaugeChart, BulletChart if custom): wrap any CSS animations with a conditional that checks `prefersReducedMotion`

**Pattern for each file:**
1. Add `ariaLabel` prop
2. Import `useReducedMotion`
3. Call hook at top of component
4. Wrap return in `<div role="img" aria-label={ariaLabel ?? "Chart type name"}>`
5. Add sr-only data table inside wrapper
6. Pass `isAnimationActive={!prefersReducedMotion}` to Recharts components
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && npx tsc --noEmit src/components/ui/charts/DonutChart.tsx src/components/ui/charts/BulletChart.tsx src/components/ui/charts/CalendarHeatmap.tsx src/components/ui/charts/GaugeChart.tsx 2>&1 | head -20 && echo "---role check---" && grep -rl "role=\"img\"" src/components/ui/charts/*.tsx | wc -l && echo "---motion check---" && grep -rl "useReducedMotion" src/components/ui/charts/*.tsx | wc -l</automated>
  </verify>
  <done>All 12 chart components have role="img" wrapper with aria-label prop, hidden data table for screen readers, and pass isAnimationActive based on useReducedMotion hook. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `grep -rl "role=\"img\"" src/components/ui/charts/*.tsx | wc -l` returns 12
- `grep -rl "useReducedMotion" src/components/ui/charts/*.tsx | wc -l` returns 12
- `grep -rn "isAnimationActive" src/components/ui/charts/*.tsx | wc -l` returns >= 12
- `grep "indigo-50" src/components/ui/StatCard.tsx` confirms default iconColor
- `grep "useReducedMotion" src/components/ui/StatCard.tsx` confirms hook usage
- `npx tsc --noEmit` passes for all modified files
</verification>

<success_criteria>
All 12 chart components are accessible (role=img, aria-label, hidden data tables) and respect reduced-motion preferences. StatCard uses indigo defaults and its animated number hook skips animation for reduced-motion users. The useReducedMotion hook is reusable across the codebase.
</success_criteria>

<output>
After completion, create `.planning/phases/06-shared-ui-components/06-04-SUMMARY.md`
</output>
