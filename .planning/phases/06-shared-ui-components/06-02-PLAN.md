---
phase: 06-shared-ui-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Alert.tsx
  - src/components/ui/Badge.tsx
  - src/components/ui/Spinner.tsx
  - src/components/ui/Toast.tsx
  - src/components/ui/Pagination.tsx
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-05, COMP-08]

must_haves:
  truths:
    - "Alert warning state shows amber colors, info state shows indigo"
    - "Badge warning uses amber, info uses indigo, all variants have border pattern"
    - "Spinner uses indigo-600 with role=status and aria-label and motion-safe prefix"
    - "Toast info uses indigo, has warning variant with amber, has motion-safe and focus-visible dismiss"
    - "Pagination active page uses indigo-600, all buttons have focus-visible:ring, slate palette"
  artifacts:
    - path: "src/components/ui/Alert.tsx"
      provides: "Amber warning, indigo info, accessible dismiss button"
      contains: "amber"
    - path: "src/components/ui/Badge.tsx"
      provides: "Amber warning, indigo info, border on all variants"
      contains: "amber"
    - path: "src/components/ui/Spinner.tsx"
      provides: "Indigo spinner with role=status"
      contains: "role"
    - path: "src/components/ui/Toast.tsx"
      provides: "Indigo info, amber warning, motion-safe animation"
      contains: "motion-safe"
    - path: "src/components/ui/Pagination.tsx"
      provides: "Indigo active page, focus-visible rings"
      contains: "indigo-600"
  key_links:
    - from: "src/components/ui/Alert.tsx"
      to: "40+ pages using Alert"
      via: "component import"
      pattern: "amber-50.*amber-400.*amber-800"
    - from: "src/components/ui/Spinner.tsx"
      to: "30+ pages using Spinner"
      via: "component import"
      pattern: "role=\"status\""
---

<objective>
Fix color tokens and accessibility on Alert, Badge, Spinner, Toast, and Pagination — replacing yellow with amber, blue with indigo, adding ARIA attributes, motion-safe prefixes, and focus-visible rings.

Purpose: These 5 components cascade to 150+ page usages. Fixing the component fixes everywhere at once. Current yellow/blue colors conflict with the design system's amber/indigo palette.
Output: 5 updated component files with correct color tokens and accessibility attributes.
</objective>

<execution_context>
@C:/Users/18cla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18cla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-ui-components/06-CONTEXT.md
@.planning/action-plan/PRIORITIES.md
@src/components/ui/Alert.tsx
@src/components/ui/Badge.tsx
@src/components/ui/Spinner.tsx
@src/components/ui/Toast.tsx
@src/components/ui/Pagination.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Alert, Badge, and Spinner color tokens and accessibility</name>
  <files>src/components/ui/Alert.tsx, src/components/ui/Badge.tsx, src/components/ui/Spinner.tsx</files>
  <action>
**Alert.tsx** (High B-01):
- Warning variant: replace any `yellow-*` with amber — `bg-amber-50 border-amber-400 text-amber-800`
- Info variant: replace any `blue-*` with indigo — `bg-indigo-50 border-indigo-400 text-indigo-800`
- Dismiss/close button: add `aria-label="Dismiss"` and `focus-visible:ring` (not `focus:ring`)
- Replace all `gray-*` with `slate-*`

**Badge.tsx** (High B-02):
- Warning variant: yellow -> amber (`bg-amber-50 text-amber-800 border-amber-200`)
- Info variant: blue -> indigo (`bg-indigo-50 text-indigo-800 border-indigo-200`)
- Add border pattern to ALL variants (success, error, warning, info, default) — each should have a `border` class with appropriate color
- Fix shade consistency on success/error variants if needed
- Replace all `gray-*` with `slate-*`

**Spinner.tsx** (High B-03):
- Replace `border-t-blue-600` with `border-t-indigo-600`
- Replace `border-gray-200` with `border-slate-200`
- Add `role="status"` to the spinner element
- Add `aria-label="Loading"` (or accept it as a prop with "Loading" default)
- Replace `animate-spin` with `motion-safe:animate-spin` so spinner respects reduced-motion
- Replace any remaining `gray-*` with `slate-*`
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && npx tsc --noEmit src/components/ui/Alert.tsx src/components/ui/Badge.tsx src/components/ui/Spinner.tsx 2>&1 | head -20 && echo "---yellow check---" && grep -c "yellow-" src/components/ui/Alert.tsx src/components/ui/Badge.tsx 2>/dev/null; echo "---blue check---" && grep -c "blue-" src/components/ui/Alert.tsx src/components/ui/Badge.tsx src/components/ui/Spinner.tsx 2>/dev/null; echo "---gray check---" && grep -c "gray-" src/components/ui/Alert.tsx src/components/ui/Badge.tsx src/components/ui/Spinner.tsx 2>/dev/null; echo "---aria check---" && grep -c "role=" src/components/ui/Spinner.tsx</automated>
  </verify>
  <done>Alert uses amber for warning and indigo for info with accessible dismiss. Badge uses amber/indigo with borders on all variants. Spinner uses indigo-600 with role=status, aria-label, and motion-safe prefix. No yellow-*, blue-*, or gray-* classes remain.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Toast and Pagination color tokens and accessibility</name>
  <files>src/components/ui/Toast.tsx, src/components/ui/Pagination.tsx</files>
  <action>
**Toast.tsx** (High B-05):
- Info variant: replace blue -> indigo (`bg-indigo-50 border-indigo-200 text-indigo-600`)
- Add warning variant using amber palette (`bg-amber-50 border-amber-200 text-amber-600` with amber icon)
- Add `motion-safe:` prefix to any animation classes (e.g., `motion-safe:animate-slide-in-from-right` or similar)
- Dismiss button: add `focus-visible:ring` (not `focus:ring`)
- Replace all `gray-*` with `slate-*`

**Pagination.tsx** (High B-08, Blocking #16):
- Active page button: replace `bg-gray-900` (or current dark bg) with `bg-indigo-600 text-white`
- Add `focus-visible:ring-2 focus-visible:ring-indigo-500 focus-visible:ring-offset-2` to ALL pagination buttons (prev, next, page numbers)
- Upgrade button dimensions: `w-9 h-9` (36px) to `min-w-[44px] min-h-[44px]` for scanner route tap target compliance
- Replace all `gray-*` with `slate-*` throughout
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && npx tsc --noEmit src/components/ui/Toast.tsx src/components/ui/Pagination.tsx 2>&1 | head -20 && echo "---blue check---" && grep -c "blue-" src/components/ui/Toast.tsx src/components/ui/Pagination.tsx 2>/dev/null; echo "---gray check---" && grep -c "gray-" src/components/ui/Toast.tsx src/components/ui/Pagination.tsx 2>/dev/null; echo "---indigo check---" && grep -c "indigo-" src/components/ui/Pagination.tsx</automated>
  </verify>
  <done>Toast info uses indigo, has amber warning variant, motion-safe animation, focus-visible dismiss. Pagination active page is indigo-600, all buttons are 44px min and have focus-visible:ring. No blue-* or gray-* classes remain.</done>
</task>

</tasks>

<verification>
- `grep -rn "yellow-\|blue-\|gray-" src/components/ui/Alert.tsx src/components/ui/Badge.tsx src/components/ui/Spinner.tsx src/components/ui/Toast.tsx src/components/ui/Pagination.tsx` returns 0 matches
- `grep -rn "role=\"status\"" src/components/ui/Spinner.tsx` returns a match
- `grep -rn "motion-safe" src/components/ui/Spinner.tsx src/components/ui/Toast.tsx` returns matches in both
- `grep -rn "warning" src/components/ui/Toast.tsx` confirms warning variant exists
- `npx tsc --noEmit` passes for all 5 files
</verification>

<success_criteria>
All 5 components use the correct design system palette (amber for warning, indigo for info/brand). Spinner has role=status and motion-safe. Toast has warning variant. Pagination buttons are 44px min with indigo active state. No yellow-*, blue-*, or gray-* palette classes remain.
</success_criteria>

<output>
After completion, create `.planning/phases/06-shared-ui-components/06-02-SUMMARY.md`
</output>
