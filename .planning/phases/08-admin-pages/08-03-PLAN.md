---
phase: 08-admin-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(internal)/inventory/transfers/page.tsx
  - src/app/(internal)/tasks/pick/page.tsx
  - src/app/(internal)/tasks/putaway/page.tsx
  - src/app/(internal)/tasks/inspection/page.tsx
  - src/app/(internal)/locations/[id]/sublocations/page.tsx
autonomous: true
requirements: [SCAN-09, SCAN-10, SCAN-11, ADMN-06]

must_haves:
  truths:
    - "Inventory Transfers page uses slate palette throughout, no size=sm action buttons, date cells are text-base"
    - "Pick Queue, Putaway Queue, and Inspection Queue pages have no size=sm action buttons"
    - "Location Sublocations page uses slate/indigo palette exclusively with focus-visible:ring-indigo-500 on all form inputs"
    - "Location Sublocations non-Blocking overrides use indigo for icons and hover states, slate for print hover"
  artifacts:
    - path: "src/app/(internal)/inventory/transfers/page.tsx"
      provides: "Transfers slate palette and tap targets"
      contains: "text-slate-"
    - path: "src/app/(internal)/tasks/pick/page.tsx"
      provides: "Pick queue tap target compliance"
    - path: "src/app/(internal)/tasks/putaway/page.tsx"
      provides: "Putaway queue tap target compliance"
    - path: "src/app/(internal)/tasks/inspection/page.tsx"
      provides: "Inspection queue tap target compliance"
    - path: "src/app/(internal)/locations/[id]/sublocations/page.tsx"
      provides: "Sublocations slate/indigo palette and focus rings"
      contains: "focus-visible:ring-indigo-500"
  key_links:
    - from: "src/app/(internal)/inventory/transfers/page.tsx"
      to: "Button component"
      via: "size prop removed"
      pattern: "Button(?!.*size=\"sm\")"
    - from: "src/app/(internal)/locations/[id]/sublocations/page.tsx"
      to: "form inputs"
      via: "focus-visible:ring-indigo-500 class"
      pattern: "focus-visible:ring.*indigo"
---

<objective>
Fix three scanner-route admin pages for palette compliance and tap targets: Inventory Transfers, Task Queues (Pick/Putaway/Inspection), and Location Sublocations.

Purpose: These scanner-route pages are used on warehouse floor devices where tap targets and readability are safety-critical. They also have the most Blocking-tier findings in the admin section.
Output: Five corrected page files with slate/indigo palette, no size="sm" buttons, and proper focus rings.
</objective>

<execution_context>
@C:/Users/18cla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18cla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-pages/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Inventory Transfers and Task Queue pages</name>
  <files>src/app/(internal)/inventory/transfers/page.tsx, src/app/(internal)/tasks/pick/page.tsx, src/app/(internal)/tasks/putaway/page.tsx, src/app/(internal)/tasks/inspection/page.tsx</files>
  <action>
**Inventory Transfers (src/app/(internal)/inventory/transfers/page.tsx):**
1. Line ~106: Change `text-gray-900` to `text-slate-900`
2. Lines ~114-116: Change `text-gray-900` to `text-slate-900`, `text-gray-400` arrow to `text-slate-400`
3. Line ~124: Change `text-gray-600` to `text-slate-600`
4. Line ~142: Change `text-gray-500 text-sm` to `text-slate-500 text-base` (fix both palette AND text size for scanner compliance)
5. Lines ~155, ~168: Remove `size="sm"` from both action buttons (Complete and Cancel buttons) so they render at default size meeting 44px tap target

**Pick Queue (src/app/(internal)/tasks/pick/page.tsx):**
1. Line ~179: Remove `size="sm"` from the Badge (this is Badge not Button, but audit flagged it — check if Badge has size prop; if so remove it)
2. Line ~252: Remove `size="sm"` from the action Button

**Putaway Queue (src/app/(internal)/tasks/putaway/page.tsx):**
1. Lines ~222, ~232: Remove `size="sm"` from both action Buttons

**Inspection Queue (src/app/(internal)/tasks/inspection/page.tsx):**
1. Lines ~212, ~223, ~311: Remove `size="sm"` from all three action Buttons
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && ! grep -n 'size="sm"' src/app/\(internal\)/inventory/transfers/page.tsx src/app/\(internal\)/tasks/pick/page.tsx src/app/\(internal\)/tasks/putaway/page.tsx src/app/\(internal\)/tasks/inspection/page.tsx | grep -v node_modules && ! grep -n "text-gray-\|text-sm.*date\|text-sm.*created" src/app/\(internal\)/inventory/transfers/page.tsx | grep -v node_modules</automated>
  </verify>
  <done>Inventory Transfers uses slate palette throughout with text-base date cells. All task queue pages have no size="sm" action buttons. All action buttons render at default size meeting 44px tap target.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Location Sublocations page palette and focus rings</name>
  <files>src/app/(internal)/locations/[id]/sublocations/page.tsx</files>
  <action>
This is the worst single page in the audit (9 Blocking findings + 3 High findings). Apply comprehensive palette replacement:

**Checkbox inputs (lines ~452, ~461):**
1. Change `text-blue-600 border-gray-300 focus:ring-blue-500` to `text-indigo-600 border-slate-300 focus-visible:ring-indigo-500`

**Table cell text (lines ~470-525):**
1. Change all `text-gray-900` to `text-slate-900`
2. Change all `text-gray-600` to `text-slate-600`
3. Change all `text-gray-400` to `text-slate-400`

**Action buttons (lines ~561, ~568):**
1. Line ~561: Change edit hover from `hover:text-blue-600 hover:bg-blue-50` to `hover:text-indigo-600 hover:bg-indigo-50`
2. Line ~568: Change print hover from `hover:text-purple-600 hover:bg-purple-50` to `hover:text-slate-600 hover:bg-slate-50` (purple to slate per user decision)
3. Both action buttons: change `text-gray-500` to `text-slate-500`

**Loading spinner (line ~582):**
1. Change `border-blue-600` to `border-indigo-600`

**Back link (line ~632):**
1. Change `text-gray-600 hover:text-gray-900` to `text-slate-600 hover:text-slate-900`

**Location summary icon (lines ~649-650):**
1. Change `bg-blue-100` to `bg-indigo-100` and `text-blue-600` to `text-indigo-600`

**Location detail text (lines ~653-668):**
1. Change all `text-gray-900` to `text-slate-900`, `text-gray-500` to `text-slate-500`, `text-gray-400` to `text-slate-400`

**Section divider and tabs (lines ~679-689):**
1. Change `border-gray-100` to `border-slate-100`
2. Change `text-gray-500` to `text-slate-500`
3. Line ~689: Change `text-blue-600 hover:text-blue-800` to `text-indigo-600 hover:text-indigo-800`

**Form labels (lines ~754-829):**
1. Change all `text-gray-700` and `text-gray-500` labels to `text-slate-700` and `text-slate-500`
2. Change all form input classes from `border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500` to `border-slate-300 rounded-md focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500` (also fix rounded-lg to rounded-md and focus:ring to focus-visible:ring)
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && ! grep -n "text-gray-\|bg-gray-\|border-gray-\|text-blue-\|bg-blue-\|border-blue-\|text-purple-\|bg-purple-\|focus:ring-blue" src/app/\(internal\)/locations/\[id\]/sublocations/page.tsx | grep -v node_modules</automated>
  </verify>
  <done>Location Sublocations uses slate/indigo palette exclusively. All form inputs use focus-visible:ring-indigo-500 with border-slate-300 and rounded-md. Edit hover uses indigo, print hover uses slate. No gray/blue/purple inline overrides remain.</done>
</task>

</tasks>

<verification>
Run the following to confirm full compliance:
```bash
# No size="sm" on any scanner-route page
grep -rn 'size="sm"' src/app/(internal)/inventory/transfers/page.tsx src/app/(internal)/tasks/pick/page.tsx src/app/(internal)/tasks/putaway/page.tsx src/app/(internal)/tasks/inspection/page.tsx

# No gray/blue/purple on sublocations page
grep -rn "gray-\|blue-\|purple-" src/app/(internal)/locations/[id]/sublocations/page.tsx
```
Expected: zero matches for both commands.
</verification>

<success_criteria>
- Inventory Transfers: slate palette, no size="sm" buttons, text-base date cells
- Pick/Putaway/Inspection queues: no size="sm" action buttons
- Location Sublocations: full slate/indigo palette, focus-visible:ring-indigo-500 on all inputs, rounded-md, no gray/blue/purple
- Zero Blocking violations remain across all 5 scanner-route admin pages
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-pages/08-03-SUMMARY.md`
</output>
