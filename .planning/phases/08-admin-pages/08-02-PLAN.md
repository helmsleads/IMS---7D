---
phase: 08-admin-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(internal)/tasks/page.tsx
  - src/app/(internal)/lots/page.tsx
  - src/app/(internal)/reports/page.tsx
  - src/app/(internal)/settings/page.tsx
  - src/app/(internal)/tasks/[id]/page.tsx
autonomous: true
requirements: [ADMN-03, ADMN-04, ADMN-05, ADMN-07, ADMN-10]

must_haves:
  truths:
    - "Tasks List page shows indigo icons for task types, uses Badge component for priority"
    - "Lots List page uses indigo tabs, count badges, search input focus ring, and lot number links"
    - "Reports Hub uses indigo icon colors with focus-visible:ring on report card links"
    - "Settings page active sidebar tab uses indigo palette"
    - "Task Detail back button is at least 44px, timestamps are text-sm, timeline uses indigo indicators"
  artifacts:
    - path: "src/app/(internal)/tasks/page.tsx"
      provides: "Tasks list indigo icons and Badge priority"
      contains: "bg-indigo-50"
    - path: "src/app/(internal)/lots/page.tsx"
      provides: "Lots list indigo tabs and search"
      contains: "border-indigo-500 text-indigo-600"
    - path: "src/app/(internal)/reports/page.tsx"
      provides: "Reports hub indigo icons"
      contains: "bg-indigo-100 text-indigo-600"
    - path: "src/app/(internal)/settings/page.tsx"
      provides: "Settings indigo active tab"
      contains: "bg-indigo-50 text-indigo-600"
    - path: "src/app/(internal)/tasks/[id]/page.tsx"
      provides: "Task detail 44px back button and indigo timeline"
      contains: "min-h-\\[44px\\]"
  key_links:
    - from: "src/app/(internal)/tasks/page.tsx"
      to: "Badge component"
      via: "import and usage for priority"
      pattern: "import Badge.*Badge"
    - from: "src/app/(internal)/tasks/[id]/page.tsx"
      to: "back button"
      via: "p-3 min-h class"
      pattern: "p-3.*min-h-\\[44px\\]"
---

<objective>
Fix inline color overrides on five admin pages: Tasks List, Lots List, Reports Hub, Settings, and Task Detail.

Purpose: These pages have blue/purple inline colors for icons, tabs, links, and timeline indicators that need to become indigo/slate per the admin palette. Task Detail also needs a 44px back button tap target.
Output: Five corrected page files with consistent indigo/slate admin palette.
</objective>

<execution_context>
@C:/Users/18cla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18cla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-pages/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Tasks List, Reports Hub, and Settings pages</name>
  <files>src/app/(internal)/tasks/page.tsx, src/app/(internal)/reports/page.tsx, src/app/(internal)/settings/page.tsx</files>
  <action>
**Tasks List (src/app/(internal)/tasks/page.tsx):**
1. Line ~57: Change putaway icon from `text-blue-600` to `text-indigo-600`
2. Lines ~306-307: Change putaway stat icon container from `bg-blue-50` to `bg-indigo-50` and icon from `text-blue-600` to `text-indigo-600`
3. If priority is rendered with inline styled spans instead of Badge component, replace with `<Badge variant="warning">High</Badge>`, `<Badge variant="error">Urgent</Badge>`, `<Badge variant="default">Normal</Badge>` pattern. Import Badge if not already imported.

**Reports Hub (src/app/(internal)/reports/page.tsx):**
1. Line ~37: Change icon color from `bg-blue-100 text-blue-600` to `bg-indigo-100 text-indigo-600`
2. Line ~61: Change icon color from `bg-purple-100 text-purple-600` to `bg-indigo-100 text-indigo-600` (purple to indigo — reports icons all use same brand color since these are functional category icons, not status indicators, so indigo is correct here rather than slate)
3. Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500 focus-visible:ring-offset-2` to the report card link/clickable elements that wrap each report category

**Settings (src/app/(internal)/settings/page.tsx):**
1. Line ~69: Change active sidebar tab from `bg-blue-50 text-blue-600 border-l-4 border-blue-600` to `bg-indigo-50 text-indigo-600 border-l-4 border-indigo-600`
2. Line ~70: Change inactive tab `text-gray-600 hover:bg-gray-50` to `text-slate-600 hover:bg-slate-50`
3. Lines ~52-101: Change all remaining `border-gray-200` to `border-slate-200`, `bg-gray-50` to `bg-slate-50`, `text-gray-500` to `text-slate-500`, `text-gray-400` to `text-slate-400`, `text-gray-300` to `text-slate-300`, `text-gray-600` to `text-slate-600`
4. Line ~141: Change save button from `bg-blue-600 hover:bg-blue-700` to `bg-indigo-600 hover:bg-indigo-700`
5. Line ~120-121: Change content area `border-gray-200` to `border-slate-200`, `text-gray-900` to `text-slate-900`
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && ! grep -n "bg-blue-\|text-blue-\|bg-purple-\|text-purple-" src/app/\(internal\)/tasks/page.tsx src/app/\(internal\)/reports/page.tsx src/app/\(internal\)/settings/page.tsx | grep -v node_modules</automated>
  </verify>
  <done>Tasks List shows indigo icons for all task types. Reports Hub uses indigo for all category icons with focus-visible:ring on links. Settings active tab uses indigo palette. No blue/purple inline overrides remain.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Lots List and Task Detail pages</name>
  <files>src/app/(internal)/lots/page.tsx, src/app/(internal)/tasks/[id]/page.tsx</files>
  <action>
**Lots List (src/app/(internal)/lots/page.tsx):**
1. Lines ~228-229, ~236-237, ~247-248, ~262-263, ~270-271: Change all active tab classes from `border-blue-500 text-blue-600` to `border-indigo-500 text-indigo-600`, inactive from `text-gray-500 hover:text-gray-700 hover:border-gray-300` to `text-slate-500 hover:text-slate-700 hover:border-slate-300`
2. Lines ~236-237, ~270-271: Change count badge from `bg-blue-100 text-blue-600` to `bg-indigo-100 text-indigo-600`, inactive from `bg-gray-100 text-gray-600` to `bg-slate-100 text-slate-600`
3. Line ~222: Change `border-gray-200` to `border-slate-200`
4. Line ~284: Change search icon `text-gray-400` to `text-slate-400`
5. Line ~290: Change search input `border-gray-300 focus:ring-blue-500` to `border-slate-300 focus-visible:ring-indigo-500` (also change focus:ring to focus-visible:ring)
6. Lines ~299-301: Change skeleton `bg-gray-200` and `bg-gray-100` to `bg-slate-200` and `bg-slate-100`
7. Lines ~330-340: Change table header `border-gray-200` and `text-gray-700` to `border-slate-200` and `text-slate-700`
8. Change lot number links from any `text-blue-*` to `text-indigo-600 hover:text-indigo-800` (search for lot number link rendering in the table rows)

**Task Detail (src/app/(internal)/tasks/[id]/page.tsx):**
1. Line ~275: Change back button from `p-2` to `p-3 min-h-[44px] min-w-[44px] inline-flex items-center justify-center` for 44px tap target
2. Lines ~417-418: Change timeline assigned indicator from `bg-blue-100` to `bg-indigo-100` and icon from `text-blue-600` to `text-indigo-600`
3. Verify timestamps already use `text-xs` per line ~412 — these should remain `text-xs` for timeline secondary info (the requirement says "text-sm timestamps" but the priority doc B-28 says `text-xs timestamps -> text-sm`). Change `text-xs text-slate-500` timestamps on lines ~412 and similar to `text-sm text-slate-500`.
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && ! grep -n "bg-blue-\|text-blue-\|border-blue-\|focus:ring-blue" src/app/\(internal\)/lots/page.tsx src/app/\(internal\)/tasks/\[id\]/page.tsx | grep -v node_modules</automated>
  </verify>
  <done>Lots List uses indigo tabs, count badges, search focus ring, and lot number links — no blue remaining. Task Detail back button is min 44x44px, timeline uses indigo indicators, timestamps are text-sm.</done>
</task>

</tasks>

<verification>
Run the following to confirm no remaining incorrect palette classes:
```bash
grep -rn "bg-blue-\|text-blue-\|border-blue-\|bg-purple-\|text-purple-" src/app/(internal)/tasks/page.tsx src/app/(internal)/lots/page.tsx src/app/(internal)/reports/page.tsx src/app/(internal)/settings/page.tsx src/app/(internal)/tasks/[id]/page.tsx
```
Expected: zero matches.
</verification>

<success_criteria>
- Tasks List: indigo task type icons, Badge component for priority
- Lots List: indigo tabs/badges/search/links, no blue classes
- Reports Hub: indigo icon colors, focus-visible:ring on card links
- Settings: indigo active sidebar tab, slate neutral palette
- Task Detail: 44px back button, text-sm timestamps, indigo timeline
- Zero remaining blue/purple inline overrides across all 5 files
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-pages/08-02-SUMMARY.md`
</output>
