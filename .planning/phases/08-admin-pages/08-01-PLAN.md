---
phase: 08-admin-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(internal)/dashboard/page.tsx
  - src/app/(internal)/inbound/page.tsx
  - src/app/(internal)/outbound/page.tsx
  - src/app/(internal)/inventory/page.tsx
autonomous: true
requirements: [ADMN-01, ADMN-02, ADMN-08, ADMN-09]

must_haves:
  truths:
    - "Dashboard StatCard icons show indigo, not blue; blob circles replaced with subtle pattern; hero buttons use rounded-md and focus-visible:ring"
    - "Inbound status tabs use amber for Ordered, indigo for In Transit, slate for Arrived — no yellow/blue/purple"
    - "Outbound status tabs use amber for Pending, indigo for Confirmed, slate for Processing — no yellow/blue/purple"
    - "Inventory List status map uses amber for quarantine, indigo for reserved, slate for returned — no yellow/blue/purple"
    - "Outbound source badges use slate for both portal and internal — no purple or cyan on admin pages"
  artifacts:
    - path: "src/app/(internal)/dashboard/page.tsx"
      provides: "Dashboard color corrections and blob replacement"
      contains: "bg-indigo-50 text-indigo-600"
    - path: "src/app/(internal)/inbound/page.tsx"
      provides: "Inbound status tab color corrections"
      contains: "bg-amber-100"
    - path: "src/app/(internal)/outbound/page.tsx"
      provides: "Outbound status tab + source badge corrections"
      contains: "bg-slate-100"
    - path: "src/app/(internal)/inventory/page.tsx"
      provides: "Inventory status map corrections"
      contains: "bg-amber-50"
  key_links:
    - from: "src/app/(internal)/dashboard/page.tsx"
      to: "StatCard component"
      via: "iconColor prop"
      pattern: "iconColor=\"bg-indigo-50 text-indigo-600\""
    - from: "src/app/(internal)/outbound/page.tsx"
      to: "source badge rendering"
      via: "inline className"
      pattern: "bg-slate-100 text-slate-600"
---

<objective>
Fix inline color overrides on the four highest-finding admin pages: Dashboard, Inbound, Outbound, and Inventory List.

Purpose: These pages contain status tab arrays, status config maps, and inline color classes that still use blue/yellow/purple instead of the indigo/amber/slate admin palette.
Output: Four corrected page files with no remaining blue/yellow/purple inline overrides.
</objective>

<execution_context>
@C:/Users/18cla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18cla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-pages/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Dashboard and Inventory List color overrides</name>
  <files>src/app/(internal)/dashboard/page.tsx, src/app/(internal)/inventory/page.tsx</files>
  <action>
**Dashboard (src/app/(internal)/dashboard/page.tsx):**
1. Line ~457: Change `iconColor="bg-blue-50 text-blue-600"` to `iconColor="bg-indigo-50 text-indigo-600"` on the StatCard
2. Lines ~393-394: Replace the two decorative blob `<div>` elements (absolute positioned rounded-full circles with `bg-indigo-500/5`) with a subtle CSS pattern. Replace both blob divs with a single div using a subtle dot-grid pattern via background-image: `<div className="absolute inset-0 opacity-[0.03]" style={{ backgroundImage: 'radial-gradient(circle, #4F46E5 1px, transparent 1px)', backgroundSize: '24px 24px' }} />`. This provides visual texture without clutter.
3. Lines ~416-443: On the 3-4 hero action `<Link>` elements, change `rounded-lg` to `rounded-md`. Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500 focus-visible:ring-offset-2` to each link.

**Inventory List (src/app/(internal)/inventory/page.tsx):**
1. Line ~56: Change quarantine status from `bgClass: "bg-yellow-100", textClass: "text-yellow-700"` to `bgClass: "bg-amber-50", textClass: "text-amber-700"`
2. Line ~57: Change reserved status from `bgClass: "bg-blue-100", textClass: "text-blue-700"` to `bgClass: "bg-indigo-50", textClass: "text-indigo-700"`
3. Line ~58: Change returned status from `bgClass: "bg-purple-100", textClass: "text-purple-700"` to `bgClass: "bg-slate-100", textClass: "text-slate-700"`
4. Lines ~611, ~847-848: Change any remaining `text-purple-500` grid icon and `bg-blue-100 text-blue-600` summary icon to `text-slate-500` and `bg-indigo-100 text-indigo-600` respectively
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && ! grep -n "bg-blue-50\|text-blue-600\|bg-yellow-100\|text-yellow-700\|bg-purple-100\|text-purple-700" src/app/\(internal\)/dashboard/page.tsx src/app/\(internal\)/inventory/page.tsx | grep -v node_modules</automated>
  </verify>
  <done>Dashboard shows indigo icons, subtle dot pattern instead of blobs, rounded-md hero buttons with focus-visible:ring. Inventory List status map uses amber/indigo/slate. No blue/yellow/purple inline overrides remain in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Inbound and Outbound list page color overrides</name>
  <files>src/app/(internal)/inbound/page.tsx, src/app/(internal)/outbound/page.tsx</files>
  <action>
**Inbound (src/app/(internal)/inbound/page.tsx):**
1. Line ~31: Change "All" tab from `bg-gray-100 text-gray-700` to `bg-slate-100 text-slate-700`
2. Line ~32: Change "Ordered" from `bg-yellow-100 text-yellow-700` to `bg-amber-100 text-amber-700`
3. Line ~33: Change "In Transit" from `bg-blue-100 text-blue-700` to `bg-indigo-100 text-indigo-700`
4. Line ~34: Change "Arrived" from `bg-purple-100 text-purple-700` to `bg-slate-100 text-slate-700` (purple to slate per user decision)
5. Lines ~383-385: Fix active/inactive status tab button classes — change `bg-gray-900` to `bg-slate-900`, `text-gray-600 hover:bg-gray-50 border-gray-200` to `text-slate-600 hover:bg-slate-50 border-slate-200`
6. Lines ~408-439: Fix date filter buttons — change `bg-blue-600` active to `bg-indigo-600`, `bg-gray-50 text-gray-600 border-gray-200 hover:bg-gray-100` to `bg-slate-50 text-slate-600 border-slate-200 hover:bg-slate-100`, `bg-gray-200 text-gray-700` badge to `bg-slate-200 text-slate-700`

**Outbound (src/app/(internal)/outbound/page.tsx):**
1. Line ~29: Change "All" tab from `bg-gray-100 text-gray-700` to `bg-slate-100 text-slate-700`
2. Line ~30: Change "Pending" from `bg-yellow-100 text-yellow-700` to `bg-amber-100 text-amber-700`
3. Line ~31: Change "Confirmed" from `bg-blue-100 text-blue-700` to `bg-indigo-100 text-indigo-700`
4. Line ~32: Change "Processing" from `bg-purple-100 text-purple-700` to `bg-slate-100 text-slate-700` (purple to slate per user decision)
5. Line ~35: Change "Delivered" from `bg-gray-100 text-gray-700` to `bg-slate-100 text-slate-700`
6. Lines ~176: Change portal source badge from `bg-purple-100 text-purple-700` to `bg-slate-100 text-slate-600` (per user decision: both source badges use slate, NO cyan on admin)
7. Lines ~183: Change internal source badge from `bg-gray-100 text-gray-600` to `bg-slate-100 text-slate-600`
8. Lines ~248, ~259, ~292: Change carrier preferred `text-purple-600` to `text-slate-600`, tracking link `text-blue-600` to `text-indigo-600`, truck icon `text-blue-600` to `text-indigo-600`
9. Lines ~363-406: Fix source filter buttons — change `bg-purple-600` portal active to `bg-slate-700`, `text-purple-200` count to `text-slate-300`, all `text-gray-*` to `text-slate-*`, `bg-gray-*` to `bg-slate-*`
10. Lines ~419-444: Fix status tab buttons — change `bg-blue-600` active to `bg-indigo-600`, all `bg-gray-*`/`text-gray-*` to `bg-slate-*`/`text-slate-*`
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && ! grep -n "bg-yellow-100\|text-yellow-700\|bg-blue-100\|text-blue-700\|bg-purple-100\|text-purple-700\|bg-purple-600\|text-purple-600\|bg-blue-600" src/app/\(internal\)/inbound/page.tsx src/app/\(internal\)/outbound/page.tsx | grep -v node_modules</automated>
  </verify>
  <done>Inbound and Outbound list pages use amber for warning states, indigo for active/info states, and slate for neutral/arrived/processing states. Outbound source badges use slate for both portal and internal. No blue/yellow/purple inline overrides remain in either file.</done>
</task>

</tasks>

<verification>
Run the following to confirm no remaining incorrect palette classes across all 4 files:
```bash
grep -rn "bg-blue-\|text-blue-\|bg-yellow-\|text-yellow-\|bg-purple-\|text-purple-" src/app/(internal)/dashboard/page.tsx src/app/(internal)/inbound/page.tsx src/app/(internal)/outbound/page.tsx src/app/(internal)/inventory/page.tsx
```
Expected: zero matches (or only intentional blue usage like link text if any exists outside status contexts).
</verification>

<success_criteria>
- Dashboard uses indigo StatCard icons, subtle dot pattern replaces blobs, hero buttons are rounded-md with focus-visible:ring
- Inbound/Outbound status tabs use amber/indigo/slate palette consistently
- Outbound source badges both use slate (no cyan on admin)
- Inventory List status map uses amber for quarantine, indigo for reserved, slate for returned
- Zero remaining blue/yellow/purple inline overrides in any of the 4 files
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-pages/08-01-SUMMARY.md`
</output>
