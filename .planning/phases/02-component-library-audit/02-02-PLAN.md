---
phase: 02-component-library-audit
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .planning/audit/components.md
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03]

must_haves:
  truths:
    - "User can open .planning/audit/components.md and find entries for all 10 scanner components with warehouse floor rubric applied"
    - "Scanner findings flag tap targets below 44px and text below 16px with appropriate severity"
    - "Cross-cutting findings (prefers-reduced-motion, token non-use patterns) documented once in Section 3"
    - "Summary counts in Overview are filled in with accurate totals by severity"
    - "User can identify which components have accessibility gaps from the completed document"
  artifacts:
    - path: ".planning/audit/components.md"
      provides: "Complete audit of all 37 components (27 shared UI + 10 scanner)"
      contains: "Section 2: Scanner Components"
  key_links:
    - from: ".planning/audit/components.md Section 2"
      to: ".planning/phases/01-tool-setup-and-design-system/SCANNER-ROUTES.md"
      via: "Scanner components mapped to their serving routes"
      pattern: "Scanner Routes|/tasks/|/inbound/|/outbound/"
    - from: ".planning/audit/components.md Section 3"
      to: "src/app/globals.css"
      via: "Cross-cutting token and animation findings"
      pattern: "prefers-reduced-motion|globals\\.css"
---

<objective>
Complete the component audit by adding scanner component findings (Section 2), cross-cutting findings (Section 3), and filling in the Overview summary counts in `.planning/audit/components.md`.

Purpose: Scanner components serve warehouse floor routes where tap target and text size violations are Blocking severity. Cross-cutting findings (prefers-reduced-motion gap, systemic token non-use) must be documented once to prevent duplication across page audits in Phase 3.

Output: Completed `.planning/audit/components.md` with all 37 components audited, cross-cutting findings, and accurate summary totals.
</objective>

<execution_context>
@C:/Users/18cla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18cla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library-audit/02-CONTEXT.md
@.planning/phases/02-component-library-audit/02-RESEARCH.md
@.planning/phases/02-component-library-audit/02-01-SUMMARY.md
@design-system/ims7d/MASTER.md
@.planning/phases/01-tool-setup-and-design-system/SCANNER-ROUTES.md
@.planning/phases/01-tool-setup-and-design-system/DASHBOARD-CONSTRAINTS.md
@.planning/audit/components.md
@src/app/globals.css

<interfaces>
<!-- Scanner components to audit (10 total) -->

From src/components/internal/:
- PickingScanner.tsx — serves /tasks/pick
- PickScanner.tsx — serves /tasks/pick (alternate)
- PackScanner.tsx — serves /outbound/[id] (packing)
- ShipScanner.tsx — serves /outbound/[id] (shipping)
- PutawayScanner.tsx — serves /tasks/putaway
- InspectionScanner.tsx — serves /tasks/inspection
- ReceivingScanner.tsx — serves /inbound/[id]
- PalletBreakdownScanner.tsx — serves /inventory/pallet-breakdown
- ScannerModal.tsx — modal wrapper for all scanner routes

From src/components/ui/:
- BarcodeScanner.tsx — shared scanner infrastructure (already audited in Plan 01 Section 1, but needs scanner rubric overlay here)

Scanner Floor Rubric (MASTER.md Section 7):
- 44px+ tap targets (Blocking if violated)
- 16px+ body text / text-base minimum (Blocking for primary action text, High for secondary)
- Maximum 3 primary actions per screen
- WCAG AA contrast minimum
- No precision gestures (no small checkboxes, no hover-dependent interactions)
- Glove-friendly: large buttons, generous spacing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit scanner components with warehouse floor rubric and complete cross-cutting section</name>
  <files>.planning/audit/components.md</files>
  <action>
Read all 10 scanner component files listed below. Apply the same 5-dimension audit as Plan 01 PLUS the MASTER.md Section 7 warehouse floor rubric as an additional hard gate.

**Scanner component files to read and audit:**
1. `src/components/internal/PickingScanner.tsx`
2. `src/components/internal/PickScanner.tsx` — research flagged uncertainty about whether this is deprecated or an alternate; read it and note usage status
3. `src/components/internal/PackScanner.tsx`
4. `src/components/internal/ShipScanner.tsx`
5. `src/components/internal/PutawayScanner.tsx`
6. `src/components/internal/InspectionScanner.tsx`
7. `src/components/internal/ReceivingScanner.tsx`
8. `src/components/internal/PalletBreakdownScanner.tsx`
9. `src/components/internal/ScannerModal.tsx`
10. `src/components/ui/BarcodeScanner.tsx` (already in Section 1 — add scanner rubric overlay findings here in Section 2 with cross-reference)

**For each scanner component, apply 5 standard dimensions PLUS scanner floor rubric:**
- **Tap targets:** Measure all interactive elements. Any button/link below 44px total height is Blocking.
- **Text sizes:** Any `text-sm` (14px) or `text-xs` (12px) in readable content areas. Blocking for primary action identifiers (product name during scan). High for secondary information.
- **Primary action count:** Flag if more than 3 primary actions visible simultaneously.
- **Contrast:** Verify text/background pairs against WCAG AA in the context of warehouse floor lighting (bright fluorescent).
- **Precision gestures:** Flag any small checkboxes, hover-dependent interactions, or drag-and-drop in scanner context.

**For each finding, record the same format as Plan 01:**
- File path and line range
- Current state, recommended change
- Root cause ID (RC-01 through RC-05, or RC-04 specifically for scanner text size, or "scanner-specific")
- Severity (use scanner severity rules: tap target < 44px = Blocking, text < 16px primary = Blocking, text < 16px secondary = High)
- Effort estimate (XS/S/M/L)

**Then write Section 3: Cross-Cutting Findings:**

Document these once-only findings that affect multiple components:

1. **globals.css: Missing `prefers-reduced-motion`** — No `@media (prefers-reduced-motion: reduce)` block. Affects: animate-modal-scale-up, animate-widget-enter, animate-chart-enter, slide-in-from-right. Severity: High. Effort: S. List all animated components that inherit this gap.

2. **Systemic gray-to-slate migration** — Summarize the RC-01 pattern: X components use `gray-*` where `slate-*` is mandated. Provide a consolidated count and list of affected components. This is the single root cause that, if fixed, eliminates the most findings across pages.

3. **Systemic token non-use (RC-02)** — Summarize which brand-critical colors are hardcoded vs tokenized. Count of components using `blue-*` where `indigo-*` or CSS custom properties should be used.

4. **Portal variant absence** — Document that Button, Input, Select, Textarea, Toggle all lack a portal color variant (cyan). Portal pages must use className overrides. Severity: Medium. Effort: M per component.

**Finally, update the Overview section:**

Go back to the top of components.md and fill in the Summary subsection:
- Total components audited: 37 (27 shared UI + 10 scanner)
- Total findings by severity: Blocking: X, High: X, Medium: X, Low: X
- Total findings count
- Top root causes by finding count

Count every finding row across all three sections to produce accurate totals.
  </action>
  <verify>
    <automated>grep -c "^### " ".planning/audit/components.md" | head -1</automated>
Verify total component headings >= 37. Also: `grep "Section 2" ".planning/audit/components.md"` confirms scanner section exists. `grep "Section 3" ".planning/audit/components.md"` confirms cross-cutting section exists. `grep "Total findings" ".planning/audit/components.md"` confirms summary counts are filled in (not placeholder). `grep "prefers-reduced-motion" ".planning/audit/components.md"` confirms cross-cutting a11y finding.
  </verify>
  <done>
- Section 2 contains entries for all 10 scanner components with warehouse floor rubric findings
- Scanner tap target violations flagged as Blocking with specific pixel measurements
- Scanner text size violations flagged with correct severity (Blocking for primary, High for secondary)
- BarcodeScanner has scanner rubric overlay cross-referenced from Section 1
- Section 3 documents prefers-reduced-motion gap, gray-to-slate migration scope, token non-use scope, and portal variant absence
- Overview summary counts are accurate totals (not placeholders)
- Complete components.md is ready for Phase 3 page audits to cross-reference
  </done>
</task>

</tasks>

<verification>
- `components.md` has Section 1 (27 shared UI), Section 2 (10 scanner), Section 3 (cross-cutting)
- Total component entries >= 37
- Scanner findings include tap target measurements and text size checks
- Cross-cutting section documents prefers-reduced-motion, gray-to-slate scope, token non-use scope
- Overview summary has numeric totals by severity (not TBD/placeholder)
- grep for "Blocking" in Section 2 returns results (scanner violations are expected)
- grep for "44px" or "tap target" returns results in scanner section
</verification>

<success_criteria>
- All 37 components have complete audit entries across 5 dimensions (COMP-01)
- Every finding has a severity rating (COMP-02)
- Accessibility gaps identified per component including scanner a11y (COMP-03)
- Cross-cutting findings documented once for Phase 3 reference
- Summary totals accurate and complete
- Document structure supports Phase 3 cross-referencing ("source: component" classification)
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-library-audit/02-02-SUMMARY.md`
</output>
