---
phase: 01-tool-setup-and-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - design-system/ims7d/MASTER.md
  - .planning/phases/01-tool-setup-and-design-system/TOKEN-VALIDATION.md
autonomous: false
requirements: [SETUP-02, DSYS-01, DSYS-02, DSYS-03]

must_haves:
  truths:
    - "User can open design-system/ims7d/MASTER.md and find a complete 3PL-specific design system"
    - "User can find separate token sections for admin (indigo) and portal (cyan) brand variants in MASTER.md"
    - "User can verify all 20 globals.css custom properties are covered in MASTER.md recommendations"
    - "User can confirm the design system references premium logistics identity (Flexport-inspired, trust and authority tone)"
  artifacts:
    - path: "design-system/ims7d/MASTER.md"
      provides: "Locked evaluation rubric for all subsequent audit phases"
      contains: "admin.*portal|portal.*admin"
    - path: ".planning/phases/01-tool-setup-and-design-system/TOKEN-VALIDATION.md"
      provides: "Mapping of all 20 globals.css custom properties to MASTER.md recommendations"
      contains: "--color-primary"
  key_links:
    - from: "design-system/ims7d/MASTER.md"
      to: "src/app/globals.css"
      via: "CSS custom property names referenced in token recommendations"
      pattern: "--color-(primary|portal|success|warning|error|info|bg|text|border)"
    - from: ".planning/phases/01-tool-setup-and-design-system/TOKEN-VALIDATION.md"
      to: "design-system/ims7d/MASTER.md"
      via: "Cross-reference of every globals.css property against MASTER.md"
      pattern: "MASTER\\.md Recommendation"
---

<objective>
Generate the 3PL/warehouse-specific MASTER.md design system with dual-brand constraints (admin indigo, portal cyan), then validate that all 20 existing globals.css custom properties are covered in the generated recommendations.

Purpose: MASTER.md is the locked evaluation rubric for all audit phases. It must exist with dual-brand sections and 3PL-specific content before any component or page auditing begins. The token validation ensures the design system maps cleanly to existing code.
Output: `design-system/ims7d/MASTER.md`, `TOKEN-VALIDATION.md`
</objective>

<execution_context>
@C:/Users/18cla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18cla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tool-setup-and-design-system/01-CONTEXT.md
@.planning/phases/01-tool-setup-and-design-system/01-RESEARCH.md
@.planning/phases/01-tool-setup-and-design-system/01-01-SUMMARY.md
@.planning/phases/01-tool-setup-and-design-system/SCANNER-ROUTES.md
@.planning/phases/01-tool-setup-and-design-system/DASHBOARD-CONSTRAINTS.md
@src/app/globals.css

<interfaces>
<!-- Key reference: existing CSS custom properties in globals.css that MASTER.md must map to -->

From src/app/globals.css:
```css
/* Admin brand */
--color-primary: #4F46E5;
--color-primary-hover: #4338CA;
--color-primary-light: #EEF2FF;

/* Portal brand */
--color-portal: #0891B2;
--color-portal-hover: #0E7490;
--color-portal-light: #ECFEFF;

/* Semantic */
--color-success: #16a34a;
--color-success-light: #f0fdf4;
--color-warning: #d97706;
--color-warning-light: #fffbeb;
--color-error: #dc2626;
--color-error-light: #fef2f2;
--color-info: #4F46E5;
--color-info-light: #EEF2FF;

/* Neutrals (slate) */
--color-bg-page: #F8FAFC;
--color-bg-subtle: #F1F5F9;
--color-bg-card: #ffffff;
--color-text-primary: #0F172A;
--color-text-secondary: #64748B;
--color-border: #E2E8F0;
--color-border-light: #F1F5F9;

/* Shadows */
--shadow-xs, --shadow-card, --shadow-card-hover, --shadow-elevated, --shadow-modal

/* Radii */
--radius-sm: 0.375rem;
--radius-md: 0.5rem;
--radius-lg: 0.75rem;
--radius-xl: 1rem;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate MASTER.md with dual-brand 3PL constraints</name>
  <files>design-system/ims7d/MASTER.md</files>
  <action>
Generate the design system using the UI/UX Max Pro skill. This is a two-step process: CLI smoke test first, then Claude-driven generation with the full constraint prompt.

**Step 1: CLI smoke test** (confirms the --design-system flag works)
```bash
python3 .claude/skills/ui-ux-pro-max/scripts/search.py "3PL warehouse management logistics" --design-system --persist -p "ims7d" --stack nextjs
```
If this succeeds, it writes a baseline `design-system/ims7d/MASTER.md`. If it fails (unrecognized flag), fall back to Claude-driven generation only.

**Step 2: Claude-driven generation with full constraint prompt**

Read the SKILL.md file to understand the skill's workflow. Then use the skill's design system generation capability with this EXACT constraint prompt (do not abbreviate or omit any constraint):

```
Generate a design system for IMS-7D, a 3PL warehouse management platform.

HARD CONSTRAINTS — do not merge or normalize these:
- Admin portal brand: deep indigo (#4F46E5). This is the internal staff interface.
- Client portal brand: cyan/teal (#0891B2). This is the external client view.
- These must remain visually distinct. Generate separate token sets for each.

VISUAL REFERENCE: Flexport — premium, authoritative, logistics-native identity.
VISUAL TONE: Trust and authority. Conservative typography, structured layouts, muted colors that inspire confidence.

USER PERSONAS:
- Office staff (admin): desktop, data-dense tables, 8+ hours/day, expert users
- Warehouse workers (admin scanner pages): tablets + phones, gloved hands, 44px+ touch targets, high contrast
- External clients (portal): desktop/mobile, occasional use, reporting focus

INDUSTRY CONTEXT: 3PL, warehouse management, logistics, supply chain, fulfillment, inventory management
STACK: Next.js 14, Tailwind CSS 4 (config-free), CSS custom properties in globals.css

EXISTING TOKEN NAMES (MASTER.md must reference these exact CSS custom property names):
--color-primary, --color-primary-hover, --color-primary-light,
--color-portal, --color-portal-hover, --color-portal-light,
--color-success, --color-success-light, --color-warning, --color-warning-light,
--color-error, --color-error-light, --color-info, --color-info-light,
--color-bg-page, --color-bg-subtle, --color-bg-card,
--color-text-primary, --color-text-secondary,
--color-border, --color-border-light,
--shadow-xs, --shadow-card, --shadow-card-hover, --shadow-elevated, --shadow-modal,
--radius-sm, --radius-md, --radius-lg, --radius-xl

REQUIRED SECTIONS:
1. Colors — separate admin (indigo) and portal (cyan) token tables
2. Typography — Google Font pairing recommendation for premium logistics identity (heading + body)
3. Spacing/Layout — data-dense office layouts + scanner-floor large-target layouts
4. Effects — shadows, radii, transitions
5. Component patterns — buttons, cards, tables, modals, inputs, badges
6. Anti-patterns — things to avoid in 3PL/warehouse context
7. Scanner/warehouse floor section — specific rubric for the 12 scanner-facing routes
8. Dashboard constraints — reference DASHBOARD-CONSTRAINTS.md minimums
```

**Step 3: Validate dual-brand output**

After generation, verify MASTER.md contains:
- Two separate color/token sections (one for admin/indigo, one for portal/cyan)
- If only one unified palette exists, the dual-brand constraint was NOT honored — regenerate with stronger constraint language
- Typography section with specific Google Font recommendation
- Scanner/warehouse floor section with 44px+ tap target rubric
- Anti-patterns section

Write the final MASTER.md to `design-system/ims7d/MASTER.md`. Create the `design-system/ims7d/` directory if it does not exist.

CRITICAL: Do NOT regenerate MASTER.md after this task. It becomes the locked rubric. All subsequent audit phases reference this exact file.
  </action>
  <verify>
    <automated>ls "design-system/ims7d/MASTER.md" &amp;&amp; grep -c "portal\|cyan\|#0891B2" "design-system/ims7d/MASTER.md" &amp;&amp; grep -c "admin\|indigo\|#4F46E5" "design-system/ims7d/MASTER.md"</automated>
  </verify>
  <done>MASTER.md exists at design-system/ims7d/MASTER.md with separate admin (indigo) and portal (cyan) token sections, 3PL-specific content, typography recommendation, scanner floor rubric, and anti-patterns. Both brand variants are present and visually distinct.</done>
</task>

<task type="auto">
  <name>Task 2: Validate MASTER.md tokens against globals.css custom properties</name>
  <files>.planning/phases/01-tool-setup-and-design-system/TOKEN-VALIDATION.md</files>
  <action>
Create a structured token validation document that maps every CSS custom property from `src/app/globals.css` to its corresponding recommendation in `design-system/ims7d/MASTER.md`.

Read both files. For each of the following 20+ custom properties, record:
1. The CSS custom property name
2. Its current value in globals.css
3. What MASTER.md recommends (exact value or description)
4. Whether there is a gap (property not covered in MASTER.md)

Create `.planning/phases/01-tool-setup-and-design-system/TOKEN-VALIDATION.md` with this structure:

```markdown
# DSYS-03: Token Validation — MASTER.md vs globals.css

**Date:** [today]
**MASTER.md version:** [first line or title from MASTER.md]
**globals.css location:** src/app/globals.css

## Validation Summary

- Total CSS custom properties: [count]
- Covered in MASTER.md: [count]
- Gaps (not covered): [count]
- Status: [PASS if 0 gaps, FAIL if any gaps]

## Admin Brand Tokens

| CSS Custom Property | Current Value | MASTER.md Recommendation | Gap? |
|--------------------|---------------|--------------------------|------|
| --color-primary | #4F46E5 | [from MASTER.md] | N |
| --color-primary-hover | #4338CA | [from MASTER.md] | N |
| --color-primary-light | #EEF2FF | [from MASTER.md] | N |

## Portal Brand Tokens

| CSS Custom Property | Current Value | MASTER.md Recommendation | Gap? |
|--------------------|---------------|--------------------------|------|
| --color-portal | #0891B2 | [from MASTER.md] | N |
| --color-portal-hover | #0E7490 | [from MASTER.md] | N |
| --color-portal-light | #ECFEFF | [from MASTER.md] | N |

## Semantic Tokens

[... all semantic color properties ...]

## Neutral Tokens

[... all neutral/background/text/border properties ...]

## Effect Tokens

[... all shadow and radius properties ...]

## Gaps (if any)

[List any properties NOT covered in MASTER.md with recommendation to add them]
```

This is read-only validation. Do NOT modify globals.css or MASTER.md. If gaps exist, document them — they will be addressed in the implementation milestone (v2), not now.
  </action>
  <verify>
    <automated>ls ".planning/phases/01-tool-setup-and-design-system/TOKEN-VALIDATION.md" &amp;&amp; grep -c "\-\-color-" ".planning/phases/01-tool-setup-and-design-system/TOKEN-VALIDATION.md"</automated>
  </verify>
  <done>TOKEN-VALIDATION.md maps all 20+ globals.css custom properties to MASTER.md recommendations. Each property shows current value, recommended value, and gap status. Summary shows total coverage count.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify MASTER.md design system and token validation</name>
  <action>
Human verifies two deliverables from Tasks 1-2:

1. Open design-system/ims7d/MASTER.md and verify:
   - Separate token sections exist for admin (indigo #4F46E5) and portal (cyan #0891B2) — they are NOT merged into one palette
   - Typography section recommends a specific Google Font pairing
   - Scanner/warehouse floor section references 44px+ tap targets and glove-friendly interactions
   - Anti-patterns section exists with 3PL-relevant warnings
   - Overall tone is "premium logistics" / "trust and authority" (not consumer SaaS)

2. Open .planning/phases/01-tool-setup-and-design-system/TOKEN-VALIDATION.md and verify:
   - All 20+ CSS custom properties from globals.css appear in the table
   - Each has a corresponding MASTER.md recommendation
   - Gaps (if any) are documented but acceptable for this audit-only phase

3. Confirm you are satisfied with MASTER.md as the locked evaluation rubric for Phases 2-4

Resume signal: Type "approved" to lock MASTER.md as the evaluation rubric, or describe issues to regenerate.
  </action>
  <verify>User confirms "approved"</verify>
  <done>User has approved MASTER.md as the locked evaluation rubric and confirmed token validation coverage is acceptable</done>
</task>

</tasks>

<verification>
- `design-system/ims7d/MASTER.md` exists and contains dual-brand token sections
- `grep -c "portal\|cyan" design-system/ims7d/MASTER.md` returns > 0
- `grep -c "admin\|indigo" design-system/ims7d/MASTER.md` returns > 0
- TOKEN-VALIDATION.md maps all 20+ globals.css custom properties
- MASTER.md references scanner floor rubric (44px+ tap targets)
- MASTER.md includes typography recommendation
</verification>

<success_criteria>
- MASTER.md exists with separate admin (indigo) and portal (cyan) brand sections — NOT a merged single palette
- MASTER.md covers colors, typography, spacing/layout, effects, component patterns, anti-patterns, and scanner floor rubric
- MASTER.md references premium logistics identity (Flexport-inspired, trust and authority)
- TOKEN-VALIDATION.md confirms all 20+ globals.css custom properties are covered (or gaps explicitly documented)
- User has approved MASTER.md as the locked evaluation rubric
</success_criteria>

<output>
After completion, create `.planning/phases/01-tool-setup-and-design-system/01-02-SUMMARY.md`
</output>
