---
phase: 07-scanner-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/internal/PickingScanner.tsx
  - src/components/internal/PickScanner.tsx
  - src/components/ui/BarcodeScanner.tsx
autonomous: true
requirements:
  - SCAN-01
  - SCAN-02
  - SCAN-07
must_haves:
  truths:
    - "PickingScanner audio toggle and Pick list buttons are at least 44px tap targets"
    - "PickingScanner has no text smaller than text-base"
    - "PickScanner audio toggle, decrement, increment, and Pick All buttons are at least 44px"
    - "PickScanner has no text smaller than text-base"
    - "BarcodeScanner close button is at least 44px and instruction text is text-base"
  artifacts:
    - path: "src/components/internal/PickingScanner.tsx"
      provides: "Tap-target and text-size compliant PickingScanner"
    - path: "src/components/internal/PickScanner.tsx"
      provides: "Tap-target and text-size compliant PickScanner"
    - path: "src/components/ui/BarcodeScanner.tsx"
      provides: "Tap-target and text-size compliant BarcodeScanner"
  key_links:
    - from: "src/components/internal/PickingScanner.tsx"
      to: "src/components/ui/Button.tsx"
      via: "Button component import (audio toggle converted from raw button)"
      pattern: "Button.*onClick.*audioEnabled"
---

<objective>
Fix tap targets and text sizes in PickingScanner, PickScanner, and BarcodeScanner to meet the 44px minimum tap target and text-base minimum text size warehouse floor accessibility standards.

Purpose: Warehouse workers using handheld scanner devices need large enough buttons to tap with gloves and large enough text to read at arm's length.
Output: Three scanner components fully compliant with SCAN-01, SCAN-02, and SCAN-07 requirements.
</objective>

<execution_context>
@C:/Users/18cla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18cla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scanner-components/07-CONTEXT.md
@.planning/action-plan/PRIORITIES.md

Reference these PRIORITIES.md items:
- Blocking #1: PickingScanner — audio toggle raw `<button p-2>` and Pick list button `size="sm"`
- Blocking #2: PickScanner — audio toggle, decrement, increment, Pick All buttons all `size="sm"`
- Blocking #7: BarcodeScanner — close button `p-1`, instruction text `text-sm`/`text-xs`
- High B-14: PickingScanner text sizes — `text-sm` on SKU, "picked" label, location hint badges, progress subtitle, "Complete" feedback text
- High B-15: PickScanner text sizes — `text-sm` on secondary product name, item count progress indicator
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PickingScanner tap targets and text sizes (Blocking #1 + B-14)</name>
  <files>src/components/internal/PickingScanner.tsx</files>
  <action>
Fix all tap targets to minimum 44px and all text to minimum text-base in PickingScanner.tsx.

**Tap target fixes (Blocking #1):**
1. Audio toggle (line ~434-442): Convert raw `<button>` with `p-2` to `<Button>` component. Use `variant="ghost"` and add `min-h-[44px] min-w-[44px]` class. Keep the Volume2 icon and conditional styling for enabled/disabled state. Change icon color classes from `bg-blue-100 text-blue-600` / `bg-gray-100 text-gray-400` to use Button ghost styling with aria-label.
   Resulting JSX pattern:
   ```
   <Button
     variant="ghost"
     onClick={() => setAudioEnabled(!audioEnabled)}
     className={`min-h-[44px] min-w-[44px] ${audioEnabled ? "text-blue-600" : "text-slate-400"}`}
     aria-label={audioEnabled ? "Sound on" : "Sound off"}
   >
     <Volume2 className="w-5 h-5" />
   </Button>
   ```
2. Pick list "Pick" button (line ~754-761): Remove `size="sm"` — defaults to `size="md"`. Add `min-h-[44px]` to ensure 44px target.

**Text size fixes (B-14) — change ALL text-sm and text-xs to text-base:**
1. Progress subtitle (line ~426-427): `text-sm text-gray-500` -> `text-base text-slate-500`
2. "Not Found" secondary text (line ~492-493): `text-sm text-gray-500` -> `text-base text-slate-500`
3. "Not in Order" secondary text (line ~508-509): `text-sm text-gray-500` -> `text-base text-slate-500`
4. "No Stock" secondary text (line ~524-525): `text-sm text-gray-500` -> `text-base text-slate-500`
5. Product SKU in found card (line ~557): `text-sm text-gray-500` -> `text-base text-slate-500`
6. Quantity stats grid (line ~560): `text-sm` on the grid container -> `text-base`
7. "Pick from location" label (line ~576): `text-sm font-medium text-gray-700` -> `text-base font-medium text-slate-700`
8. Location availability text (line ~592-593): `text-sm text-gray-500` -> `text-base text-slate-500`
9. "Add to pick quantity" label (line ~602): `text-sm font-medium text-gray-700` -> `text-base font-medium text-slate-700`
10. "Pending to Pick" label (line ~639): `text-sm text-blue-600` -> `text-base text-blue-600`
11. "New Total" label (line ~643): `text-sm text-gray-500` -> `text-base text-slate-500`
12. "Complete" indicator (line ~649): `text-xs text-green-600` -> `text-base text-green-600`
13. Idle state instruction (line ~680-681): `text-sm` -> `text-base`
14. Pick list item SKU (line ~743): `text-sm text-gray-500` -> `text-base text-slate-500`
15. "picked" label (line ~751): `text-xs text-gray-500` -> `text-base text-slate-500`
16. Location hint badges (line ~773): `text-xs bg-gray-100 text-gray-600` -> `text-base bg-slate-100 text-slate-600`
17. "+N more" text (line ~780): `text-xs text-gray-400` -> `text-base text-slate-400`
18. "Complete" success text (line ~450): `text-sm font-medium` -> `text-base font-medium`
19. All Items Picked completion text (line ~800): keep as-is (already text-xl/text-gray-500 which are fine)

Also upgrade the `w-4 h-4` Volume2 icon to `w-5 h-5` since the button is now larger.

Do NOT touch gray->slate or blue->indigo color changes beyond what is noted above — those are deferred to Polish backlog. Only fix text sizes and tap targets.
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && npx tsc --noEmit src/components/internal/PickingScanner.tsx 2>&1 | head -20; echo "---"; grep -c "text-xs\|text-sm" src/components/internal/PickingScanner.tsx; echo "---"; grep -c "size=\"sm\"" src/components/internal/PickingScanner.tsx; echo "---"; grep -c "min-h-\[44px\]" src/components/internal/PickingScanner.tsx</automated>
  </verify>
  <done>PickingScanner has zero text-xs/text-sm instances, zero size="sm" buttons, audio toggle uses Button component with min-h-[44px], Pick list button has min-h-[44px]. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix PickScanner tap targets and text sizes (Blocking #2 + B-15)</name>
  <files>src/components/internal/PickScanner.tsx</files>
  <action>
Fix all tap targets to minimum 44px and all text to minimum text-base in PickScanner.tsx.

**Tap target fixes (Blocking #2):**
1. Audio toggle button (line ~406-411): Remove `size="sm"`. Add `min-h-[44px] min-w-[44px]`. Add `aria-label={audioEnabled ? "Sound on" : "Sound off"}`.
2. Decrement "-" button (line ~506): Remove `size="sm"`. Add `min-h-[44px] min-w-[44px]`.
3. Increment "+" button (line ~508): Remove `size="sm"`. Add `min-h-[44px] min-w-[44px]`.
4. "Pick All" button (line ~509): Remove `size="sm"`. Add `min-h-[44px]`.

**Text size fixes (B-15) — change ALL text-sm and text-xs to text-base:**
1. Item count progress (line ~403-404): `text-sm text-gray-500` -> `text-base text-slate-500`
2. Current item SKU (line ~459): `text-sm text-gray-600` -> `text-base text-slate-600`
3. Lot number text (line ~461): `text-xs text-slate-500` -> `text-base text-slate-500`
4. Suggested location (line ~464): `text-sm text-blue-600` -> `text-base text-blue-600`
5. "picked" label (line ~479): `text-sm text-gray-500` -> `text-base text-slate-500`
6. Quantity label "Quantity to pick:" (line ~504): `text-sm text-gray-600` -> `text-base text-slate-600`
7. Pick list secondary product name (line ~566): `text-sm text-gray-500` -> `text-base text-slate-500`
8. "All Items Picked" secondary text (line ~449): keep text-gray-500, change to text-base if not already

Do NOT touch gray->slate or blue->indigo color changes beyond what is noted — those are deferred to Polish backlog.
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && npx tsc --noEmit src/components/internal/PickScanner.tsx 2>&1 | head -20; echo "---"; grep -c "text-xs\|text-sm" src/components/internal/PickScanner.tsx; echo "---"; grep -c "size=\"sm\"" src/components/internal/PickScanner.tsx; echo "---"; grep -c "min-h-\[44px\]" src/components/internal/PickScanner.tsx</automated>
  </verify>
  <done>PickScanner has zero text-xs/text-sm instances, zero size="sm" buttons, all interactive elements have min-h-[44px]. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Fix BarcodeScanner tap targets and text sizes (Blocking #7)</name>
  <files>src/components/ui/BarcodeScanner.tsx</files>
  <action>
Fix BarcodeScanner close button tap target and instruction text sizes.

**Tap target fix (Blocking #7):**
1. Torch button (line ~357-371): Already `w-11 h-11` (44px) — no change needed. Verify.
2. BarcodeScannerModal close button (line ~438-446): Already `w-11 h-11` (44px) — no change needed. Verify.
3. BarcodeScannerModal footer Close button (line ~468-474): Already has `min-h-[48px]` — no change needed. Verify.

**Text size fixes (Blocking #7) — per user decision, everything text-base (no exceptions):**
1. Instruction text (line ~375): `text-sm text-slate-600` -> `text-base text-slate-600` ("Position barcode within the frame")
2. Secondary instruction (line ~378): `text-xs text-slate-400` -> `text-base text-slate-400` ("Hold steady for best results")
3. "Checking camera..." text (line ~259): `text-sm text-slate-600` -> `text-base text-slate-600`
4. "Allow camera access" text (line ~274-275): `text-sm text-slate-500` -> `text-base text-slate-500`
5. "Camera Access Denied" description (line ~295-296): `text-sm text-slate-500` -> `text-base text-slate-500`
6. "Camera Error" description (line ~316-317): `text-sm text-slate-500` -> `text-base text-slate-500`
7. Camera error banner text (line ~333): `text-sm text-red-600` -> `text-base text-red-600`
8. "Scanner inactive" secondary (line ~388-389): `text-sm text-slate-500` -> `text-base text-slate-500`
9. "Starting camera..." text (line ~350): `text-sm font-medium` -> `text-base font-medium`
10. BarcodeScannerModal "Scanned:" text (line ~459): `text-sm text-green-800` -> `text-base text-green-800`

Note: BarcodeScanner already uses slate (not gray) from Phase 6 cleanup. No gray->slate needed.
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && npx tsc --noEmit src/components/ui/BarcodeScanner.tsx 2>&1 | head -20; echo "---"; grep -c "text-xs\|text-sm" src/components/ui/BarcodeScanner.tsx; echo "---"; grep "min-h-\[4" src/components/ui/BarcodeScanner.tsx | head -5</automated>
  </verify>
  <done>BarcodeScanner has zero text-xs/text-sm instances. Torch and modal close buttons are already 44px+. All instruction text is text-base. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
After all tasks, run:
```bash
# Verify no small text remains in all three files
grep -c "text-xs\|text-sm" src/components/internal/PickingScanner.tsx src/components/internal/PickScanner.tsx src/components/ui/BarcodeScanner.tsx

# Verify no size="sm" buttons remain
grep -c 'size="sm"' src/components/internal/PickingScanner.tsx src/components/internal/PickScanner.tsx

# Verify TypeScript compiles
npx tsc --noEmit src/components/internal/PickingScanner.tsx src/components/internal/PickScanner.tsx src/components/ui/BarcodeScanner.tsx 2>&1 | head -30
```
</verification>

<success_criteria>
- PickingScanner: All buttons >= 44px, all text >= text-base, audio toggle uses Button component
- PickScanner: All buttons >= 44px (no size="sm"), all text >= text-base
- BarcodeScanner: Close button >= 44px, all instruction text >= text-base
- All three files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-scanner-components/07-01-SUMMARY.md`
</output>
