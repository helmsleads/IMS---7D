---
phase: 07-scanner-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/internal/PackScanner.tsx
  - src/components/internal/ShipScanner.tsx
  - src/components/internal/ReceivingScanner.tsx
autonomous: true
requirements:
  - SCAN-03
  - SCAN-04
  - SCAN-05
must_haves:
  truths:
    - "PackScanner audio toggle and remove-from-carton buttons are at least 44px tap targets"
    - "PackScanner has no text smaller than text-base"
    - "ShipScanner audio toggle button is at least 44px tap target"
    - "ShipScanner has no text smaller than text-base"
    - "ReceivingScanner audio toggle, lot scan trigger, and calendar reset buttons are at least 44px"
    - "ReceivingScanner has no text smaller than text-base"
    - "ReceivingScanner calendar input uses Input component with indigo focus ring"
  artifacts:
    - path: "src/components/internal/PackScanner.tsx"
      provides: "Tap-target and text-size compliant PackScanner"
    - path: "src/components/internal/ShipScanner.tsx"
      provides: "Tap-target and text-size compliant ShipScanner"
    - path: "src/components/internal/ReceivingScanner.tsx"
      provides: "Tap-target and text-size compliant ReceivingScanner"
  key_links:
    - from: "src/components/internal/ReceivingScanner.tsx"
      to: "src/components/ui/Input.tsx"
      via: "Input component for calendar date field (replacing raw input)"
      pattern: "Input.*type.*date"
---

<objective>
Fix tap targets and text sizes in PackScanner, ShipScanner, and ReceivingScanner to meet the 44px minimum tap target and text-base minimum text size warehouse floor accessibility standards.

Purpose: Warehouse workers using handheld scanner devices need large enough buttons to tap with gloves and large enough text to read at arm's length.
Output: Three scanner components fully compliant with SCAN-03, SCAN-04, and SCAN-05 requirements.
</objective>

<execution_context>
@C:/Users/18cla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18cla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scanner-components/07-CONTEXT.md
@.planning/action-plan/PRIORITIES.md

Reference these PRIORITIES.md items:
- Blocking #3: PackScanner — audio toggle `size="sm"` and `-1` remove-from-carton button `size="sm"`
- Blocking #4: ShipScanner — audio toggle `size="sm"`
- Blocking #5: ReceivingScanner — audio toggle raw `<button p-2>`, lot scan trigger, reset calendar button need `min-h-[44px]`
- High B-16: PackScanner text sizes — `text-sm` on carton count, carton item count, carton stage, product name under SKU
- High B-17: ShipScanner text sizes — `text-sm` on order number, progress text, carton status, carrier/tracking labels
- High B-18: ReceivingScanner text sizes — `text-sm` on SKU, remaining qty, lot form labels; calendar input purple focus ring
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PackScanner tap targets and text sizes (Blocking #3 + B-16)</name>
  <files>src/components/internal/PackScanner.tsx</files>
  <action>
Fix all tap targets to minimum 44px and all text to minimum text-base in PackScanner.tsx.

**Tap target fixes (Blocking #3):**
1. Audio toggle button (line ~341-348): Remove `size="sm"`. Add `min-h-[44px] min-w-[44px]`. Add `aria-label={audioEnabled ? "Sound on" : "Sound off"}`.
2. Remove-from-carton "-1" button (line ~396-400): Remove `size="sm"`. Add `min-h-[44px]`.

**Text size fixes (B-16) — change ALL text-sm and text-xs to text-base:**
1. Carton count (line ~338-339): `text-sm text-gray-500` -> `text-base text-slate-500`
2. Carton item count (line ~380): `text-sm text-gray-500` -> `text-base text-slate-500`
3. "Scan items to add" instruction (line ~407): `text-sm text-gray-500` -> `text-base text-slate-500`
4. Items to Pack product name (line ~465): `text-sm text-gray-500` -> `text-base text-slate-500`
5. Completed cartons stage text (line ~492): `text-sm text-gray-500` -> `text-base text-slate-500`
6. "No cartons found" secondary text (line ~342-343): `text-sm` -> `text-base` (if present)
7. Any remaining `text-sm` or `text-xs` instances throughout the file -> `text-base`

Do NOT touch gray->slate or blue->indigo color changes beyond what is noted — those are deferred to Polish backlog.
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && npx tsc --noEmit src/components/internal/PackScanner.tsx 2>&1 | head -20; echo "---"; grep -c "text-xs\|text-sm" src/components/internal/PackScanner.tsx; echo "---"; grep -c "size=\"sm\"" src/components/internal/PackScanner.tsx</automated>
  </verify>
  <done>PackScanner has zero text-xs/text-sm instances, zero size="sm" buttons. Audio toggle and remove button have min-h-[44px]. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ShipScanner tap targets and text sizes (Blocking #4 + B-17)</name>
  <files>src/components/internal/ShipScanner.tsx</files>
  <action>
Fix all tap targets to minimum 44px and all text to minimum text-base in ShipScanner.tsx.

**Tap target fix (Blocking #4):**
1. Audio toggle button (line ~240-245): Remove `size="sm"`. Add `min-h-[44px] min-w-[44px]`. Add `aria-label={audioEnabled ? "Sound on" : "Sound off"}`.

**Text size fixes (B-17) — change ALL text-sm and text-xs to text-base:**
1. Order number (line ~237-238): `text-sm text-gray-500` -> `text-base text-slate-500`
2. Progress "Cartons verified" text (line ~270-271): `text-sm text-gray-600` -> `text-base text-slate-600`
3. Carton count text (line ~272): `text-sm` -> `text-base`
4. Carton status "Verified"/"Pending" (line ~330-331): `text-sm` -> `text-base`
5. "No cartons found" secondary text (line ~342): `text-sm` -> `text-base`
6. Carrier label (line ~352): `text-sm text-gray-600` -> `text-base text-slate-600`
7. Tracking number label (line ~362): `text-sm text-gray-600` -> `text-base text-slate-600`
8. "Scan all cartons" footer text (line ~392): `text-sm text-center text-gray-500` -> `text-base text-center text-slate-500`
9. Any remaining `text-sm` or `text-xs` instances throughout the file -> `text-base`

Do NOT touch gray->slate or blue->indigo color changes beyond what is noted — those are deferred to Polish backlog.
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && npx tsc --noEmit src/components/internal/ShipScanner.tsx 2>&1 | head -20; echo "---"; grep -c "text-xs\|text-sm" src/components/internal/ShipScanner.tsx; echo "---"; grep -c "size=\"sm\"" src/components/internal/ShipScanner.tsx</automated>
  </verify>
  <done>ShipScanner has zero text-xs/text-sm instances, zero size="sm" buttons. Audio toggle has min-h-[44px]. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Fix ReceivingScanner tap targets and text sizes (Blocking #5 + B-18)</name>
  <files>src/components/internal/ReceivingScanner.tsx</files>
  <action>
Fix all tap targets to minimum 44px and all text to minimum text-base in ReceivingScanner.tsx.

**Tap target fixes (Blocking #5):**
1. Audio toggle (line ~338-346): Convert raw `<button>` with `p-2` to `<Button>` component. Use `variant="ghost"` and add `min-h-[44px] min-w-[44px]` class. Add `aria-label={audioEnabled ? "Sound on" : "Sound off"}`. Change icon to `w-5 h-5`.
   Resulting JSX pattern:
   ```
   <Button
     variant="ghost"
     onClick={() => setAudioEnabled(!audioEnabled)}
     className={`min-h-[44px] min-w-[44px] ${audioEnabled ? "text-blue-600" : "text-slate-400"}`}
     aria-label={audioEnabled ? "Sound on" : "Sound off"}
   >
     <Volume2 className="w-5 h-5" />
   </Button>
   ```
2. Lot scan trigger button (line ~497): Already a `<Button>` with `variant="secondary"`. Remove any `size` prop if present. Add `min-h-[44px]` to ensure 44px target.
3. Cancel Scan button (line ~479-485): Remove `size="sm"`. It already has `className="w-full"`.
4. "Max" button for lot qty (line ~551-554): Remove `size="sm"`. Add `min-h-[44px]`.
5. Calendar reset button (line ~524-531): Convert raw `<button>` to `<Button variant="ghost">` with `min-h-[44px] min-w-[44px]`. Keep Calendar icon.
6. Pick list "Receive"/"Enter Lot" buttons (line ~799-805): Remove `size="sm"`. Add `min-h-[44px]`.

**Calendar input fix (B-18 — calendar input to use Input component with indigo focus ring):**
1. Date input (line ~518-523): Replace raw `<input type="date">` with `<Input type="date">` component. This gives it proper styling with indigo focus ring via the design system. Remove the inline `border-gray-300 rounded-md focus:ring-2 focus:ring-purple-500 focus:border-purple-500` classes since Input handles its own styling.

**Text size fixes (B-18) — change ALL text-sm and text-xs to text-base:**
1. Progress subtitle (line ~330-331): `text-sm text-gray-500` -> `text-base text-slate-500`
2. "Complete" indicator (line ~354): `text-sm font-medium` -> `text-base font-medium`
3. "Not Found" secondary text (line ~396-397): `text-sm text-gray-500` -> `text-base text-slate-500`
4. "Not Expected" secondary text (line ~412-413): `text-sm text-gray-500` -> `text-base text-slate-500`
5. SKU in found product card (line ~623): `text-sm text-gray-500` -> `text-base text-slate-500`
6. Quantity stats grid (line ~626): `text-sm` -> `text-base`
7. "Add to received quantity" label (line ~642): `text-sm font-medium text-gray-700` -> `text-base font-medium text-slate-700`
8. "Pending to Add" label (line ~677): `text-sm text-blue-600` -> `text-base text-blue-600`
9. "New Total" label (line ~682): `text-sm text-gray-500` -> `text-base text-slate-500`
10. "Complete" indicator (line ~686): `text-xs text-green-600` -> `text-base text-green-600`
11. Idle state instruction (line ~717): `text-sm` -> `text-base`
12. SKU in lot entry card (line ~451): `text-sm text-gray-500` -> `text-base text-slate-500`
13. "Remaining:" text (line ~452-453): `text-sm text-gray-600` -> `text-base text-slate-600`
14. Lot tracking badge text (line ~446): `text-xs font-medium` -> `text-base font-medium`
15. Lot form labels (lines ~468, ~509, ~537): `text-sm font-medium text-gray-700` -> `text-base font-medium text-slate-700`
16. Expiration auto-calc hint (line ~511-513): `text-gray-400 font-normal` — ensure text-base
17. Summary lot/expires text (lines ~564-569): `text-sm` -> `text-base`
18. "received" label (line ~796): `text-xs text-gray-500` -> `text-base text-slate-500`
19. Pick list SKU (line ~788): `text-sm text-gray-500` -> `text-base text-slate-500`
20. Lot tracking icon badge in list (line ~781): `text-xs font-medium` -> `text-base font-medium`
21. "Qty to Receive" label (line ~573): `text-sm` -> `text-base` (if applicable)

Do NOT touch gray->slate or blue->indigo color changes beyond what is noted — those are deferred to Polish backlog. Exception: the purple focus ring on the calendar input DOES get fixed to indigo (via Input component) per B-18.
  </action>
  <verify>
    <automated>cd "C:/Users/18cla/IMS - 7D" && npx tsc --noEmit src/components/internal/ReceivingScanner.tsx 2>&1 | head -20; echo "---"; grep -c "text-xs\|text-sm" src/components/internal/ReceivingScanner.tsx; echo "---"; grep -c "size=\"sm\"" src/components/internal/ReceivingScanner.tsx; echo "---"; grep "focus:ring-purple\|ring-purple" src/components/internal/ReceivingScanner.tsx | head -5; echo "---"; grep -c "min-h-\[44px\]" src/components/internal/ReceivingScanner.tsx</automated>
  </verify>
  <done>ReceivingScanner has zero text-xs/text-sm instances, zero size="sm" buttons, zero purple focus rings. Audio toggle and calendar reset use Button component. Date input uses Input component. All interactive elements have min-h-[44px]. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
After all tasks, run:
```bash
# Verify no small text remains in all three files
grep -c "text-xs\|text-sm" src/components/internal/PackScanner.tsx src/components/internal/ShipScanner.tsx src/components/internal/ReceivingScanner.tsx

# Verify no size="sm" buttons remain
grep -c 'size="sm"' src/components/internal/PackScanner.tsx src/components/internal/ShipScanner.tsx src/components/internal/ReceivingScanner.tsx

# Verify no purple focus rings remain in ReceivingScanner
grep "purple" src/components/internal/ReceivingScanner.tsx

# Verify TypeScript compiles
npx tsc --noEmit src/components/internal/PackScanner.tsx src/components/internal/ShipScanner.tsx src/components/internal/ReceivingScanner.tsx 2>&1 | head -30
```
</verification>

<success_criteria>
- PackScanner: All buttons >= 44px (no size="sm"), all text >= text-base
- ShipScanner: Audio toggle >= 44px (no size="sm"), all text >= text-base
- ReceivingScanner: All buttons >= 44px, audio toggle and calendar reset use Button component, date input uses Input component (no purple focus ring), all text >= text-base
- All three files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-scanner-components/07-02-SUMMARY.md`
</output>
