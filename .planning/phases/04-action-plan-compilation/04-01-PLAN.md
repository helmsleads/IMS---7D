---
phase: 04-action-plan-compilation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/action-plan/PRIORITIES.md
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02

must_haves:
  truths:
    - "User can open PRIORITIES.md and find a Blocking tier with exactly 20 or fewer items"
    - "User can open PRIORITIES.md and find a High-value tier with exactly 40 or fewer items"
    - "User can open PRIORITIES.md and find a Polish backlog with no cap"
    - "User can find a distinct Quick Wins list within PRIORITIES.md"
    - "User can see each portal component variant (Button, Input, Select, Textarea, Toggle) as separate Blocking items"
    - "User can see each scanner component with tap-target violations as separate Blocking items"
  artifacts:
    - path: ".planning/action-plan/PRIORITIES.md"
      provides: "Severity-tiered action plan with capped tiers and quick wins"
      contains: "## Blocking Tier"
  key_links:
    - from: ".planning/action-plan/PRIORITIES.md"
      to: ".planning/audit/components.md"
      via: "Finding cross-references (resolves count per item)"
      pattern: "resolves.*finding"
    - from: ".planning/action-plan/PRIORITIES.md"
      to: ".planning/audit/admin-pages.md"
      via: "Finding cross-references"
      pattern: "resolves.*finding"
    - from: ".planning/action-plan/PRIORITIES.md"
      to: ".planning/audit/portal-pages.md"
      via: "Finding cross-references"
      pattern: "resolves.*finding"
---

<objective>
Create PRIORITIES.md — the severity-tiered action plan that consolidates 524 raw findings from three audit files into capped tiers (Blocking <=20, High-value <=40) plus an uncapped Quick Wins list and Polish backlog.

Purpose: This is the core synthesis deliverable — 524 raw findings must become actionable items without losing information or violating tier caps. The multiplier principle (one component fix resolves dozens of page findings) is the key consolidation mechanism.

Output: `.planning/action-plan/PRIORITIES.md`
</objective>

<execution_context>
@C:/Users/18cla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18cla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-action-plan-compilation/04-CONTEXT.md
@.planning/phases/04-action-plan-compilation/04-RESEARCH.md

# Source audit files (PRIMARY — must read all three)
@.planning/audit/components.md
@.planning/audit/admin-pages.md
@.planning/audit/portal-pages.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read and inventory all audit findings</name>
  <files>.planning/action-plan/PRIORITIES.md</files>
  <action>
Read all three source audit files completely:
- `.planning/audit/components.md` (195 findings, 37 components)
- `.planning/audit/admin-pages.md` (213 findings, 62 pages)
- `.planning/audit/portal-pages.md` (116 findings, 29 pages)

Build a mental consolidation map using the root cause taxonomy (RC-01 through RC-05, CC-01 through CC-05) from the research. Track which raw findings consolidate into which action items.

Then create `.planning/action-plan/PRIORITIES.md` with ALL four sections:

**Section 1: Blocking Tier (cap: 20 items)**
Use the exact 20 items identified in 04-RESEARCH.md "Blocking Tier Candidates" section:
- 7 scanner safety violations (one per scanner component)
- 5 portal brand identity violations (one per component: Button, Input, Select, Textarea, Toggle — per locked decision requiring per-component granularity)
- 3 scanner page tap-target violations (Inventory Transfers, Task queues, Location Sublocations)
- 1 Pagination on scanner routes
- 2 portal inline blue violations
- 1 Modal accessibility (role="dialog" + aria-modal)
- 1 SearchSelect ARIA combobox

Each Blocking item row MUST include:
- Item number (1-20)
- Item name (action-oriented, not finding-oriented)
- Root cause ID (RC-xx or CC-xx or "inline")
- Files affected (specific `src/` paths)
- Effort estimate (XS/S/M from audit files — do NOT re-estimate)
- "Resolves" count (how many raw findings this single item closes)

**Section 2: High-Value Tier (cap: 40 items)**
Use the ~40 items identified in research:
- 8 component color corrections
- 5 shared component accessibility fixes
- 5 scanner component text size fixes
- 10 admin page inline color corrections
- 7 portal page inline color corrections
- 5 cross-cutting fixes (globals.css, gray→slate, dark mode, StatusBadge)
Each row same format as Blocking.

If more than 40 High items exist after consolidation, overflow items go to Polish backlog. Do NOT downgrade any Blocking item to make room.

**Section 3: Quick Wins**
Distinct list within PRIORITIES.md (not a separate file). Quick wins = items from Blocking or High tiers that also meet: XS or S effort + resolves 5+ raw findings OR affects 10+ pages. Use the 15 candidates identified in research as the starting point. No cap on this list. Each item references its tier (Blocking or High) and item number.

**Section 4: Polish Backlog**
All Medium and Low severity findings that did not make it into Blocking or High tiers. Group by root cause for readability. No cap. Briefer format acceptable (item name, root cause, effort — no "resolves" count needed).

**Critical rules:**
- Severity first — do not promote a Medium finding to High just because it affects many pages
- Do not consolidate what the user said to keep separate (portal component variants must be 5 separate Blocking items)
- Scanner tap targets and text size violations are always Blocking or High per MASTER.md Section 7
- Every item must be an ACTION (e.g., "Add portal variant to Button component") not a FINDING (e.g., "Button lacks portal variant")
- Include a summary header with total item counts per tier and total raw findings resolved
  </action>
  <verify>
    <automated>ls ".planning/action-plan/PRIORITIES.md" && grep -c "^|" ".planning/action-plan/PRIORITIES.md"</automated>
  </verify>
  <done>PRIORITIES.md exists with Blocking tier (<=20 items), High-value tier (<=40 items), Quick Wins list, and Polish backlog. Each Blocking/High item has item number, name, root cause, files, effort, and resolves count. Portal component variants appear as 5 separate Blocking items. Scanner components appear as separate items.</done>
</task>

</tasks>

<verification>
1. Count Blocking tier items — must be <= 20
2. Count High-value tier items — must be <= 40
3. Verify Quick Wins section exists as distinct list
4. Verify Polish Backlog section exists
5. Verify 5 separate portal component variant items in Blocking tier (Button, Input, Select, Textarea, Toggle)
6. Verify scanner safety items appear in Blocking tier (7 scanner component items)
7. Verify every item has a file path reference
</verification>

<success_criteria>
- PRIORITIES.md exists at `.planning/action-plan/PRIORITIES.md`
- Blocking tier contains exactly 20 or fewer items
- High-value tier contains exactly 40 or fewer items
- Quick Wins list is present as a distinct section
- Polish backlog captures all remaining Medium/Low findings
- No information lost — every raw finding is accounted for in some tier
- Per-component granularity honored for portal variants and scanner tap targets
</success_criteria>

<output>
After completion, create `.planning/phases/04-action-plan-compilation/04-01-SUMMARY.md`
</output>
