---
phase: 04-action-plan-compilation
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - .planning/action-plan/IMPLEMENTATION.md
autonomous: true
requirements:
  - PLAN-03
  - PLAN-04

must_haves:
  truths:
    - "User can open IMPLEMENTATION.md and find each action item with a specific file path, the exact change required, and an effort estimate"
    - "User can see a wave-based implementation sequence where components come before pages"
    - "User can find suggested implementation phases for the next milestone"
    - "User can hand IMPLEMENTATION.md directly to the next milestone as input without re-synthesis"
  artifacts:
    - path: ".planning/action-plan/IMPLEMENTATION.md"
      provides: "Sequenced implementation roadmap with file paths, changes, and effort estimates"
      contains: "## Wave"
  key_links:
    - from: ".planning/action-plan/IMPLEMENTATION.md"
      to: ".planning/action-plan/PRIORITIES.md"
      via: "Item cross-references (Blocking #N, High #N)"
      pattern: "Blocking.*#|High.*#"
    - from: ".planning/action-plan/IMPLEMENTATION.md"
      to: "src/components/ui/"
      via: "Specific file paths for each action item"
      pattern: "src/components/"
    - from: ".planning/action-plan/IMPLEMENTATION.md"
      to: "src/app/"
      via: "Specific page file paths for each action item"
      pattern: "src/app/"
---

<objective>
Create IMPLEMENTATION.md — the sequenced implementation guide that takes every item from PRIORITIES.md and adds specific file paths, exact change descriptions, effort estimates, and wave ordering for the next milestone.

Purpose: This file is the bridge between audit findings and implementation. A developer (or Claude) reading this file should know exactly what to change, in what file, in what order, without needing to re-read the audit files.

Output: `.planning/action-plan/IMPLEMENTATION.md`
</objective>

<execution_context>
@C:/Users/18cla/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18cla/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-action-plan-compilation/04-CONTEXT.md
@.planning/phases/04-action-plan-compilation/04-RESEARCH.md

# Plan 01 output (must exist before this plan runs)
@.planning/action-plan/PRIORITIES.md

# Source audit files (for file path extraction and change details)
@.planning/audit/components.md
@.planning/audit/admin-pages.md
@.planning/audit/portal-pages.md

# Plan 01 summary for context
@.planning/phases/04-action-plan-compilation/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wave-structured IMPLEMENTATION.md</name>
  <files>.planning/action-plan/IMPLEMENTATION.md</files>
  <action>
Read PRIORITIES.md (created by Plan 01) and the three source audit files. Create `.planning/action-plan/IMPLEMENTATION.md` with the following structure:

**Header section:**
- Title and purpose statement
- Total items count and total effort estimate
- Wave dependency diagram (text-based): Wave 0 → Wave 1 → Wave 2 → Wave 3/4 (parallel)
- Explanation that each wave is independently mergeable as a PR

**Wave 0: Globals (1 file)**
- `src/app/globals.css`: Add `@media (prefers-reduced-motion: reduce)` block
- Include the exact CSS from 04-RESEARCH.md Code Examples section
- Effort: S
- This wave has no dependencies and unblocks animation-related findings across all subsequent waves

**Wave 1: Shared UI Components (`src/components/ui/`)**
For each component listed in PRIORITIES.md Blocking and High tiers that lives in `src/components/ui/`, create a row with:
- File path: exact `src/components/ui/ComponentName.tsx` path
- Changes: bullet list of specific changes (e.g., "Add `portal` variant to variant map: `from-cyan-500 to-teal-600`", "Replace `gray-200` with `slate-200` in border class", "Add `role='dialog'` and `aria-modal='true'` to container div")
- Priority ref: which PRIORITIES.md item(s) this addresses (e.g., "Blocking #8, High #3")
- Effort: XS/S/M (from audit files)
- Order components by: Blocking items first, then High items, then by impact (most pages affected first)

Use the code example patterns from 04-RESEARCH.md:
- Portal variant pattern (Button example)
- focus:ring → focus-visible:ring pattern
- gray → slate pattern (RC-01)
- Modal ARIA pattern

Expected ~20 components in this wave (Button, Alert, Badge, Breadcrumbs, Card, Input, Select, Textarea, Toggle, Modal, Pagination, SearchSelect, Spinner, StatCard, StatusBadge, Toast, Skeleton, Table, ProductImage, Charts x13 as one grouped item).

**Wave 2: Scanner Components (`src/components/internal/`)**
For each scanner component in PRIORITIES.md Blocking tier:
- File path: exact path (e.g., `src/components/internal/PickingScanner.tsx`)
- Changes: specific button/text changes (e.g., "Remove `size='sm'` from Pick button", "Change `text-sm` to `text-base` on SKU display")
- Priority ref: which Blocking/High item(s)
- Effort: S/M
- Include the scanner tap target pattern from 04-RESEARCH.md
- Order by: most Blocking violations first (PalletBreakdownScanner, ReceivingScanner, PickingScanner, etc.)

Expected ~10 scanner components.

**Wave 3: Admin Pages (`src/app/(internal)/`)**
Group admin page fixes into sub-sections:
- 3a: Scanner routes (highest priority — Inventory Transfers, Task queues, Location Sublocations, etc.)
- 3b: Non-scanner pages with inline overrides (Lots List, Reports Hub, Dashboard, Tasks List, status tab pages)
- 3c: Component-propagation-only pages (just need verification after Wave 1, no manual fixes)

For each page:
- File path: exact route path (e.g., `src/app/(internal)/inventory/transfers/page.tsx`)
- Changes: specific inline class replacements
- Priority ref: which item(s)
- Effort: XS/S
- Note which fixes are "auto-resolved by Wave 1" vs "requires inline change"

**Wave 4: Portal Pages (`src/app/(portal)/`)**
Group portal page fixes into sub-sections:
- 4a: Inline-override pages (blue bleed, indigo inline — must fix manually)
- 4b: Auth pages (forgot-password, reset-password — background + raw inputs)
- 4c: Component-propagation-only pages (verify after Wave 1)

For each page:
- File path: exact route path
- Changes: specific inline class replacements
- Priority ref: which item(s)
- Effort: XS/S

**Suggested Implementation Phases section:**
At the end, include a "Suggested Implementation Phases for v2.0 Milestone" section that recommends:
- How many phases/PRs the next milestone should have (recommend 4-5 based on the wave structure)
- What each phase should contain
- Estimated total effort per phase
- Dependencies between phases
- This section should be concrete enough that `/gsd:new-milestone` for v2.0 can consume it directly

**Cross-reference validation:**
- Every Blocking item from PRIORITIES.md must appear in at least one wave
- Every High item from PRIORITIES.md must appear in at least one wave
- Polish backlog items should be noted in a brief "Polish items" sub-section within each wave where applicable

**Critical rules:**
- Every row MUST have a `src/` file path — no generic descriptions
- Use effort estimates from the audit files, not new estimates
- Wave 1 must complete before Waves 3/4 begin (document this dependency)
- Waves 3 and 4 can run in parallel (document this)
- Include the multiplier effect note on component items ("Resolves N findings across M pages")
  </action>
  <verify>
    <automated>ls ".planning/action-plan/IMPLEMENTATION.md" && grep -c "src/" ".planning/action-plan/IMPLEMENTATION.md"</automated>
  </verify>
  <done>IMPLEMENTATION.md exists with Wave 0-4 structure. Every action item has a specific src/ file path, exact change description, and effort estimate. Suggested implementation phases section present. Wave dependencies documented. All Blocking and High items from PRIORITIES.md accounted for.</done>
</task>

</tasks>

<verification>
1. IMPLEMENTATION.md exists at `.planning/action-plan/IMPLEMENTATION.md`
2. Contains Wave 0, Wave 1, Wave 2, Wave 3, Wave 4 sections
3. Every action item row contains a `src/` file path
4. Every action item has an effort estimate (XS/S/M/L)
5. Every action item has a specific change description (not just "fix colors")
6. Cross-reference: every Blocking item from PRIORITIES.md appears in a wave
7. Cross-reference: every High item from PRIORITIES.md appears in a wave
8. Wave dependency statement present (Wave 1 before 3/4; 3 and 4 parallel)
9. "Suggested Implementation Phases" section present at end
</verification>

<success_criteria>
- IMPLEMENTATION.md exists with wave-structured implementation guide
- All Blocking and High items from PRIORITIES.md have corresponding implementation entries
- Every entry has file path + change description + effort estimate
- Wave dependencies clearly documented
- Suggested implementation phases section is concrete enough for v2.0 milestone planning
- A developer could pick up any wave section and implement it without reading the original audit files
</success_criteria>

<output>
After completion, create `.planning/phases/04-action-plan-compilation/04-02-SUMMARY.md`
</output>
